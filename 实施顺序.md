# phpo 项目实施顺序规划

> **文档类型**：实施计划（派生文档，从属 AGENTS.md 总纲 v2.7.0）
> **依据**：AGENTS.md §10 里程碑（M0–M7）、§5.13 Docker 清洁机制、§5.14 离线缓存机制、§3.4 编码行为准则
> **唯一界面来源**：`前端唯一界面来源.txt`（3383 行，index.html 与其同步，仅差 3 行头部注释）
> **冲突裁决**：本文与 AGENTS.md 冲突时，一律以 AGENTS.md 为准

---

## 0. 总原则

- **依赖方向**：纯函数层 → 校验层 → 存储层 → 引擎层 → 缓存层 → 任务层 → 服务层 → UI 闭环。严格单向（§0.2 规则 12）。
- **阶段门禁**：每阶段进入条件为上一阶段验收清单全绿；退出条件为本阶段验收项 + 对应单测/集成测试通过。
- **测试先行**（§3.4.4）：每个校验/机制先落地 `test/fixtures/*.yaml` 用例（preflight、端口顺延、缓存命中、密码、版本），调试至通过才算完成。
- **三段式铁律**：所有写操作走 preflight → task → applyStateChange，禁止旁路（硬红线 5）。
- **离线优先铁律**：任何镜像/扩展安装自第一天即走「先定位**当前生效缓存根**（未自定义 = `./offline/`，自定义后以用户所填为唯一根）→ 装前必查 → 命中零网络 → 镜像未命中先探本机镜像库（已有即零网络重建缓存）→ 否则临时下载 → 成功提升 → 无论成败清临时」（硬红线 8；决策 23）。

---

## 阶段 1 · M1 骨架

| 序 | 工作项 | 落点 | 验证方式 |
|---|--------|------|---------|
| 1.1 | 工程初始化：`go.mod`（module phpo, go 1.27）、`wails.json`、`Taskfile.yml`、`main.go`/`app.go` | 根目录 | `wails3 dev` 起窗 |
| 1.2 | 前端工程：Vite 5 + Vue 3.5 + TS 5 + Pinia + Router，10 路由对齐原型 `NAV`/`VIEWS` | `frontend/` | 路由全可达 |
| 1.3 | 原型 1:1 迁移：6 主题 CSS、11 视图、12 模态拆为 Vue 组件；`THEMES`(6)/`UI_SCALE`(7 档)/`CMD_ITEMS`(20) 进 constants | `views/` `components/` `styles/themes/` | 界面与 `index.html` 逐屏对照 |
| 1.4 | 事件总线：`internal/app/emitter.go` + 前端 `useStateSync`（后端事件先由 mock 触发） | 双侧 | §5.6 全部 17 个事件名可收发 |
| 1.5 | UI 偏好持久化：主题/语言/布局/缩放走 localStorage（§3.1 原则 5） | `prefsStore.ts` | 重启后偏好保留 |

**验收**：原型界面 1:1 在 Wails 窗口运行，含托盘、命令面板、日志抽屉（等效命令标注「仅展示」，§1.4）。抽屉为**左日志 / 右队列两栏**，占比**默认** 70%／30%、中缝可拖拽（40–80 夹取、双击复位，比例属 UI 偏好走 `localStorage`），左上标签固定「服务」；§5.6 事件除 `update:progress` 外逐行进抽屉日志，无任务归属者走**系统日志通道**（§5.6.1、§5.6.2、决策 24）。

---

## 阶段 2 · M2 底座（纯逻辑层，全部可单测、不碰 Docker）

| 序 | 工作项 | 落点 | 验证方式 |
|---|--------|------|---------|
| 2.1 | 路径纯函数：`derivePaths`/`hostToContainer`/`MOUNTS`(5) 从原型直译；**三根可自定义且互斥唯一**——`offline_root`/`backup_root`/每服务版本 `data_dir` 非空即完全取代默认派生值、置空即回落（§5.12、决策 24） | `internal/config/paths.go` `configstore.go` | 单测逐条对照原型输出；`TestRootOverrides_CustomWinsExclusively`/`TestDataDirOverride_OnlyAffectsDataMount` |
| 2.2 | 版本校验：仅路径安全（非空 / 无`/` `\` / 无`..` / 无`\x00` / ≤128 / trim，§5.4） | `config/versions.go` | 边界用例（含 128 上限） |
| 2.3 | 密码策略：`DEFAULT_PASSWORD=123456`、可空、零校验、明文读写 | `config/password.go` | 空密码/超长密码用例 |
| 2.4 | preflight 后端化：**19 action** + **NEEDS_HOME(17)** + PF 错误码 + `{ok,errors,warnings,adjusted}` 结构（原型 17 个 + `root-set` + `cache-import`；`errs` 码 **28**） | `internal/preflight/` | 19 个 action 各正反用例 + `TestActionAndNeedsHomeCounts` 锁计数 |
| 2.5 | 端口引擎：站点端口默认 80 → 占用顺延 1–65535 首个可用（不报错、无上限）、服务端口占用报错、冲突检测含既有站点 | `pkg/port/` + `store/port.go` | 占用矩阵用例 |
| 2.6 | SQLite（modernc，CGO_ENABLED=0）+ migrate 0001–0005 + snapshot/sync | `internal/store/` | 迁移升降级 + 一致性测试 |
| 2.7 | 三段式写操作骨架：preflight → task → applyStateChange | `internal/task/manager.go` | 绕过路径静态检查脚本 |
| 2.8 | 引擎命名层：`phpo-{kind}-{version}` 容器 / `phpo-network` / `phpo-{kind}-{version}-{purpose}` 卷（§5.13.2） | `pkg/dockerutil/naming.go` | `scripts/check-docker-naming.go` |
| 2.9 | 离线缓存核心：lookup/promote/tempdir/verifier/manifest（三铁律 + 4 必清时机，§5.14.4） | `internal/cache/` | `cache_*_test.go` 全套（hit/miss/损坏/取消/启动扫描） |
| 2.10 | 升级检查骨架（仅 checker，不做安装） | `internal/updater/checker.go` | github_mock 用例 |

**验收**：§10 M2 底座清单 + 单测全绿；CI 跑 `check-i18n-keys` / `check-templates` / `check-cache-manifest`。

---

## 阶段 3 · M3 服务线（第一次接真实 Docker）

| 序 | 工作项 | 关键点 | 验证 |
|---|--------|--------|------|
| 3.1 | Docker SDK 接入 + 启动前 Docker 可用性检查 | `engine/client.go` | 硬红线 7 生效用例 |
| 3.2 | 镜像安装全链路：查缓存 → 命中 `docker load` → 未命中先探本机镜像库（已有即 `docker save` 零网络重建）→ 没有才 `pull` → `save` → SHA256 校验 → 提升 → 清临时（§5.14.3） | 严格离线优先：缓存 > 本机镜像库 > 网络 | 断网 E2E：有缓存装 PHP 8.4 零网络；缓存丢失但本机有镜像同样零网络成功 |
| 3.3 | 容器生命周期 start/stop + Pre-Clean/Execute/Post-Verify 三阶段（§5.13.3） | 幂等 8 操作先落 install/uninstall/start/stop | 重复操作 100 次无脏资源 |
| 3.4 | 状态校准 calibrate（启动 + 每任务后 + 手动，§5.13.9） | stopped 反向推导 | 手动 kill 容器后校准一致 |
| 3.5 | Nginx 服务线：conf 模板渲染 + `nginx -t` 门禁 | `internal/template/` | 坏配置被拦用例（硬红线 2） |
| 3.6 | 任务引擎真化：日志行 5 类型协议、取消、回滚 | `task/emitter.go` `rollback.go` | 取消后临时目录必清验证 |

**验收**：PHP 任意版本（如 7.4–8.4）真实装/卸/启/停；`cache:hit/miss/promote/tempdir-cleared` 事件前端可见。

---

## 阶段 4 · M4 站点闭环（首个完整用户价值链）

| 序 | 工作项 |
|---|--------|
| 4.1 | `site add`：VHosts 9 方法后端化 + `defaultVhost` 模板 + hosts 写入（三平台提权，§8） |
| 4.2 | 端口顺延 UI：`adjusted.port` → DangerConfirm 展示（对齐原型 site-add 警告确认流程） |
| 4.3 | PHP 切换 10 步流程（§5.5）：`php-{version}-fpm:9000` 精确替换，禁止模糊匹配（硬红线 1） |
| 4.4 | 伪静态 9 预设 + vhost 编辑 + WWW_ROOT 外路径警告放行（§5.12） |
| 4.5 | 站点删除走回收站（7 天，§5.13.7）+ 删卷二次确认 |

**验收**：`demo.test` 建站 → 访问 → 切 PHP 8.3↔8.4 → vhost 上游逐字符正确；80 被占自动落 81 且 UI 明示。

---

## 阶段 5 · M5 数据服务（MySQL / PostgreSQL / Redis）

复用 M3 管线，重点增量：

- 密码明文行内编辑 + 👁 显示 / 📋 复制 / ↺ 重置（§1.5 六规则，含空密码写入）；
- 卸载保留数据卷；删 volume 必须二次确认（§0.2 规则 20）；
- `my.cnf` / `postgresql.conf` + `pg_hba.conf` / `redis.conf` 模板 + `service-config` save 链路；
- 三服务镜像安装全部走缓存优先（复用 2.9）。

**验收**：三服务装启停卸 + 空密码可用 + 重装复用卷数据不丢。

---

## 阶段 6 · M6 运维面

| 序 | 工作项 |
|---|--------|
| 6.1 | PHP 扩展 sync：apk/pecl 缓存优先 + 编译 + 提升 + 清临时 + 重建镜像重启容器（§5.14.2 第二流程） |
| 6.2 | 备份/恢复：tar.gz + 备份脱敏规范 + 导入前清空目标命名空间（§5.13.11） |
| 6.3 | doctor 15 项全量（§5.7），含缓存完整性、临时目录残留、孤儿扫描、状态漂移 |
| 6.4 | 应用升级闭环：启动时 + 24h 检查 + SHA256 + Ed25519 双校验（硬红线 6）+ 三平台 installer + 回滚 |
| 6.5 | 清理三模式（保守/标准/激进）+ 回收站 UI + 审计日志 `<用户数据目录>/logs/operations.log`（Linux `~/.config/phpo/logs/`，JSON Lines） |
| 6.6 | OfflineView 真数据化：统计、全部校验、清理、`CacheHitBadge` |

**验收**：画像 F 场景端到端——断网装全套环境；风险清单 R1–R87 逐条回访。

---

## 阶段 7 · M7 发布

- 三平台安装包：`phpo-setup-x64.exe`（NSIS）/ `phpo-x64.dmg`（含公证）/ AppImage + deb/rpm，全部附 `.sig` + `checksums.txt`；
- 代码签名与公证流程（`scripts/sign-release.sh`）；
- `release.yml` 一键出包；
- `docs/` 全中文文档定稿（§11.2 清单 30 篇）。

---

## 并行轨（不阻塞主线）

- **i18n/主题常量**：阶段 1 迁移时一次性从原型 `MESSAGES`（约 300+ 键）提取，后续只增量维护；
- **CI 与 `scripts/check-*.go` 校验**：随阶段 2 起逐步上线，每阶段末跑全量；
- **文档**：经用户裁决（2026-09-19），`docs/` 全 30 篇专项文档 + 用户手册 + CHANGELOG 统一留到 M7 由 T704 一次性写完，不再分散到各阶段。本条覆盖原「不留到 M7 补写」表述。

---

## 关键排序理由

1. **缓存核心（2.9）放在接 Docker 之前**：所有安装链路第一天就走缓存优先，避免后期回填改造（违反即触 §0.2 规则 21）。
2. **站点闭环（M4）紧随服务线**：这是画像 A/B 的「3 秒核心操作」价值链，最早可交付试用版本。
3. **扩展编译（6.1）放 M6 而非 M3**：依赖「重建镜像」能力且是最高风险网络路径，留到缓存/临时目录机制被 M3–M5 反复验证之后。
4. **纯逻辑层（M2）零 Docker 依赖**：CI 无 Docker 环境也能全量跑单测，保证 §3.4.4 目标驱动执行可落地。

---

**基线状态（2026-09-18）**：M0 规格冻结已完成——AGENTS.md v2.7.0 冻结生效；原型 5 项冲突修复 + 3 项残留修补（validateVersion ≤128、`passwordVisible` 初始 true、删除死键 `common.shortPassword`）已落地，`index.html` 与 SSOT 同步且 `node --check` 通过。
