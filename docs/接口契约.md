# 接口契约

> 对应 AGENTS.md §0.2（三段式写操作 / 后端唯一权威）/ §5.5–§5.9 / §5.13 / §5.14.10 / 工单 T704。本文描述前端可调用的**唯一绑定入口** `app.go` 的 `*App` 门面方法契约，以及所有写操作必须遵守的三段式与 preflight 前置。冲突以 AGENTS.md 为准。

## 1. 契约总则

1. **唯一入口**：`app.go` 的 `*App` 是 `wails3 generate bindings` 暴露给前端的**全部**方法；前端不直接触达 `internal/`。
2. **未就绪守卫**：`AppService`/各子服务为 nil 时统一返回 `errNotReady`（"服务尚未初始化"），防止启动钩子完成前抢跑。
3. **后端唯一权威（硬红线 4）**：读方法即时返回权威态；写方法成功后由后端发射 `state:changed`，前端订阅回流，**不做本地乐观更新**。
4. **三段式写操作（硬红线 5）**：所有写操作走 `preflight() → task.Manager（Pre-Clean → Execute → Post-Verify）→ applyStateChange`。其中第一段的**最终裁决已接进后端**：`app.go` 的 `guard(action, Ctx)`（内部 `runGuard`）在 `errNotReady` 守卫之后、提交任务之前调 `preflight.Run`，非 `Ok` 即返回错误、任务不创建。详见 §1.1。

5. **生命周期**：`Attach(app, window)` 注入 Wails 实例与主窗口并装托盘（窗口必须先建：托盘点击与「关闭时最小化」都要绑到它）；`ServiceStartup()` 执行启动钩子（含残留临时目录清理 + 状态校准）；`ServiceShutdown()` 收尾。

### 1.1 后端最终裁决（§0.2 #14）

- **接线位置**：`app.go`。校验层在 `internal/preflight/`，按 §3.1 原则 2 的单向依赖链排在 `task`/`service` **之上**（… → 校验 → 任务 → 服务 → 更新），所以 `service` 反向 import 校验层即违反 §0.2 #12——门神只能落在门面这一层，前端 `usePreflight.ts` 由此回归「仅即时反馈」的定位。
- **覆盖范围**：§0.3 冻结的 **20 个 action（原型 17 + 生产 `root-set`/`cache-import`/`docker-source-set`）全部有调用点**（21 个 `guard()` 站点，`update-config` 有两处）。`TestGateEveryActionWiredInFacade` 读 `app.go` 源码逐一对账，漏接或新增未接线的 action 即失败。
- **裁决输入取自权威快照**：`runGuard` 先 `GetState()` 拿 `*model.Snapshot` 组装 `preflight.World`，占用表与前端镜像同源（`store.CollectUsedPorts`）；不另立判据。
- **只消费 `Errors`**：`Warnings` 一律不阻断（§0.2 #16），其展示仍由前端镜像负责；`Adjusted`（站点端口顺延结果）后端不消费，写路径本就用同一张占用表重新派生，两套标准反而会漂移。
- **附属清单按需读取**：备份/缓存清单只在真消费它们的 action（`restore`、`backup-delete`、`offline-prune`）才读，读取失败只让该 action 失败闭合，不影响 `install` 等无关写操作。
- **被拒时的前端表现**：真实宿主下任务由后端执行、经事件回流，`start()` 只登记任务参数（`args` 仅供 demo 回放与日志正文，界面不再外显等效命令，AGENTS.md §1.4）并展开抽屉，因此后端拒绝不会留下「运行中」的幽灵记录，直接以 toast 呈现裁决文本。
- **刻意不接的两处**：`Reinstall`（§3：裁决是「未安装即拒 + 新端口在 Pre-Clean 前判占用」，由 `LifecycleService.Reinstall` 就地兑现，且 17 条里没有「重装」这个 action）、`SiteAddHosts`（§4：唯一前置是站点存在，无端口/路径/版本可裁决）。

## 2. 生命周期与信息

| 方法 | 契约 |
|------|------|
| `AppInfo() map[string]string` | 返回 `{name:"phpo", version}`，确认绑定链路已通 |
| `GetState() (*Snapshot, error)` | 拉取当前权威快照（订阅外的兜底读） |
| `Running() bool` | 是否有任务执行中（单飞判据；排队项不计） |
| `Cancel()` | 取消当前运行中任务（清空临时目录，见 §5.14.4） |
| `CancelQueued(id string) bool` | 撤回**尚未执行**的排队项；`false`=已移交执行或不存在，调用方不得据此改本地状态 |
| `SetOfflineRoot(ctx, root string) error` / `SetBackupRoot(ctx, root string) error` / `SetServiceDataDir(ctx, kind, version, dir string) error` | **可自定义根的唯二写入口**（需求 1/2/7/8，AGENTS.md §5.15.3）。走 `guard(root-set) → ConfigStore.Set* → container.Rebind → GetState → state:changed`：**不建任务**（它改的是配置图本身，不是业务实体），但**队列忙即拒**（`a.Running()` 为真返回 `errs.TaskBusy`——中途换图会把在跑任务持有的门面抽掉，违反 §5.13.1 原子性）。**空串＝清除自定义**并回落默认根，绝不出现「自定义与默认两条并存」。`Rebind` 按 `rootsKey`（两根 + 缓存根 + 备份根 + 排序后的数据目录）幂等：值没变就不换图 |
| `OfflineImportFile(ctx, kind, version, extType, srcPath string) error` | **手工导入缓存条目**（需求 1，§5.14.9a）。`guard(cache-import) → OfflineService.ImportEntry`：走任务（type `cache-import`，两步「读取源文件 / 写入缓存」），成功后发 `cache:promote` 并 `auditOp` 落审计。**语义是复制**（`placeFile(keepSrc=true)`，原件留在用户选的位置，与「提升＝搬走」相对），**不 `docker load`、不建容器**（缓存与 Docker 资源解耦，§5.14.13）。源文件不存在 → `errs.FileMissing` |
| `SetTrayPrefs(showTray, minimizeOnClose bool) error` | 把两项托盘偏好投影到原生外壳（撤/挂托盘图标 + 关闭窗口驻留或退出）。**UI 偏好的权威仍是 `localStorage`**（§3.1 原则 5），后端不反向读取、不落 `config.yaml`/SQLite，故是快照外的唯一例外；托盘未安装（`Attach` 前）返回 `errNotReady`。**不过 preflight**：20 个 action 已冻结且其语义无端口/路径/版本可裁决，三段式（硬红线 5）约束的是业务状态写操作 |
| `Quit() error` | 退出整个进程：托盘菜单「退出 phpo」的**唯一**出口（原生菜单只广播 `ui:command(quit)`，由前端 `AppTrayMenu.vue` 过忙锁后经 `api/state.ts#quitApp` 调用）。与 `Restart()` 的差别仅在不置 `restartPending`——退出后不再拉起自身；宿主未注入（`Attach` 前）返回 `errNotReady`，调用方不得当作已退出。**同样不过 preflight**：退出不写业务状态，20 个 action 无裁决对象 |

任务队列详情不是方法而是快照字段：`GetState()`/`state:changed` 的 `tasks{ running, pending }`（`model.TaskBoard`）。历史任务读 `OperationList(limit)` 中带 `taskId` 的行（见 §6.5）。

缺失态同样是快照字段而非方法：`Snapshot.Gaps`（`[]ServiceGap{ kind, version, reason, ref }`，总纲 §5.19.3）。它是**派生态**——`store.SetGaps` 挂到快照、**不落 SQLite**（落库即伪造第二份权威），零缺失时仍序列化为 `[]`（§5.6.3）。`reason` 冻结为 `container` / `image` / `extensions_image` 三种；前端遇未知取值原样透出枚举名。

## 3. 服务生命周期（M3）

`Install (ctx, kind ServiceKind, version string, opts InstallOptions) error` + `Reinstall / Start / Stop / Remove (ctx, kind ServiceKind, version string) error` + `Calibrate(ctx) error`。

- `Install`：缓存优先镜像（§5.14.3）→ 容器装配；幂等（先清理同名/冲突资源）。`opts{ port, password, hasPassword }` 是**安装期配置**：端口与密码只在「建容器那一刻」被 spec 读走，而 `update-config` 要求版本已安装（未装态报 `notInstalled`），所以二者必须由 `Install` 携带——门面先跑 `install` 裁决（端口占用即 `portInUse`，不顺延，§5.8），通过后才落 `config.yaml`（`services.{kind}.{version}.{port,password}`），再进任务链建容器。`hasPassword` 用于区分「未提供」与「提供空密码」（空密码合法，§1.5）；`port<=0` 视为未提供，不写脏值。安装弹窗里填的 PHP 扩展是另一条链路：安装任务之后再发一个 `ExtApply` 任务（容器内编译 → 固化 `phpo/php:{ver}` → 重建）。
- `Reinstall`：**重建容器**，把只有建容器时才落定的配置送进去——宿主端口发布 + 密码 `env`（§5.13.4 幂等「重装」）。与 `Install` 共用镜像准备/落盘/建容器三段，差别只在末段走 `LifecycleService.Reinstall`。两条前置裁决：① 快照 `Installed` 里没有该版本即拒（Docker 里有裸容器不算已装）；② **占用预检在 Pre-Clean 之前**——以 `store.CollectUsedPorts` 判 mysql/pgsql/redis 的发布端口是否被占（含注册表默认端口补档），冲突返回 `portInUse` 且**不碰任何容器**，避免"已删旧容器却绑不上新端口"。数据卷与宿主目录保留。
- `Stop`：停止但保留数据；`Remove`：卸载容器**保留数据卷**（删卷需 §5.13 二次确认路径）。
- `Start`：启动前先对 pgsql 就地执行 `healPgLogging`（§5.18.2）——旧装机磁盘上仍是「`logging_collector = on` + 往宿主 logs 目录建文件」的 `postgresql.conf`，容器会被 `unless-stopped` 无限重启、界面上永远启不来；`prepareService` 只在装/重建时跑且**保留既有配置不覆盖**，所以修复必须挂在「启用」上。写入用 `WriteFile` **原地截断**：`postgresql.conf` 是单文件 bind，换成新 inode 容器读到的仍是旧那份。启动必须等到**稳定 running**（`waitRunning`：running 后复验 2s，超时 12s），失败消息带退出码与容器日志尾部（§5.18.3）。
- `Calibrate(ctx) error`：**手动「同步状态」= 全量口径**（总纲 §5.19.2，v2.9.14）。门面名沿用（bindings 已生成），实现是 `AppService.Calibrate → LifecycleService.SyncAll → calibrate(ctx, true)`，在轻量校准之上逐个已安装版本再核**容器** + **该版本实际会跑的那一份镜像**（php 有固化镜像即以 `phpo/php:{ver}` 为准，没有才回基座 `php:{ver}-fpm`）+ **php 的扩展固化镜像**。三项都只**上报缺失态**（进快照 `Gaps`）+ 抽屉逐行点名，**绝不自动改 `installed`、不自动删建任何东西**（配置/卷/缓存都在，点「启用」即按当前配置幂等重建）。探针报错必须上抛（静默当成「本机没有」比不报更糟）。启动与每任务后仍走轻量的 `LifecycleService.Calibrate`（`auditImages=false`，只判容器存在性、复用本次已取到的实际态，零额外 Docker 调用）。**幂等闸门**：`len(res.Corrections)==0 && sameGaps(snap.Gaps, gaps)` 即静默返回、不发事件——不得用 `res.Changed()`（容器缺席是常态化的，用它当闸门等于每次同步都刷屏）。
- 全部经 `AppService → task.Manager` 三段式；失败自动回滚。`Install / Start / Stop / Remove` 分别接 `install`、`service-start`、`service-stop`、`uninstall` 四个 action 的后端裁决（§1.1）。**不新增 preflight action**（§0.3 现值 **20**；M3 这条链路本身一颗也未新增——多出的三颗是后续需求 `root-set`／`cache-import`／`docker-source-set`）：重建复用 `update-config`（`field=port`）的占用裁决，最终裁决仍在 `Reinstall` 的 Pre-Clean 之前。

## 4. 站点（M4）

| 方法 | 契约要点 |
|------|---------|
| `SiteAdd(ctx, AddInput)` | 幂等建站：建目录 → 写 vhost（**nginx -t 必过**，硬红线 2）→ 加 hosts → 发布端口 → 落库并广播 `state:changed`。任一步失败按逆序回滚，**含撤下本步刚写入的 hosts 条目**（站点没建成就不留解析；「已存在跳过」的条目不算本次改动，回滚不动它） |
| `SiteRemove(ctx, domain)` | 根目录入回收站（7 天）→ 删 vhost → **回收该域名的 hosts 条目** → 落库删除。回收步骤失败只降级为警告（提权被拒等，与 `SiteAddHosts` 同口径），不判整个删除任务失败；回滚时把条目重新加回 |
| `SiteSetPort(ctx, domain, port)` | 写 vhost `listen` + nginx -t → 落库；端口 1–65535 |
| `SiteSwitchPHP(ctx, domain, php)` | 上游精确 `php-{version}-fpm:9000`（硬红线 1），10 步流程（§5.5） |
| `SiteSetRewrite(ctx, domain, preset, rule)` | 伪静态预设（9 种）+ 自定义规则 |
| `SiteSetVhostContent(ctx, domain, content)` | 手改并保存 vhost 正文，落库前 nginx -t 必过 |
| `SiteAddHosts(ctx, domain)` → `(warning string, err error)` | 手动补写系统 hosts（站点列表「加 hosts」）。**首返回值是人话警告不是错误**：空串=已生效或本就幂等；非空=未落盘原因（如「请以管理员身份运行」），按 §3.2 原则 7 降级为提示、`err` 仍为 `nil`。仅 `hosts` 文件 IO 真失败才回 `err`。走 `task.Manager` 三段式（硬红线 5），`Apply` 段广播快照，`site.hosts` 由后端探针回流（硬红线 4）。**不新增 preflight action**（§0.3 冻结 17 条）：唯一前置是站点存在，无端口/路径/版本可裁决 |

## 5. 数据服务 env 与配置（M5）

| 方法 | 契约要点 |
|------|---------|
| `GetServicePassword / SetServicePassword` | 明文密码读写；未设置回落默认 `123456`；**空串/任意长度合法，零校验零加密**（§5.2） |
| `GetServicePort / SetServicePort` | 服务宿主发布端口读写；服务端口占用报 `portInUse`，不自动顺延（§5.8）。`Set` 只落 `config.yaml`，**运行中的容器不变**（端口发布只在建容器时定），生效需 `Reinstall`（§3）。两个 `Set` 均接 `update-config`（`field` 分别为 `password` / `port`）的后端裁决：服务未安装即拒，端口占用即拒（§1.1） |
| `ConfigGetFiles(kind, version) ([]template.File, error)` | 宿主已存在则回显内容，否则回落模板默认 |
| `ConfigSaveFiles(ctx, kind, version, files)` | 原子保存：备份原文件 → 写入 → 失败自动回滚（路径穿越防御，硬红线 3） |

## 6. 扩展 / 备份 / doctor / 升级 / 清理 / 缓存 / 向导（M6）

### 6.1 扩展
- 安装期勾选的扩展**不进 `InstallOptions`**（它只有 `port` / `password` / `hasPassword` 三项）：安装弹窗把集合放进 preflight 的 `Ctx.Extensions`（逗号串，`rules_service.go` 逐个过 `ValidateExt`），真实写入则是安装任务成功后**另起第二个任务**调 `ExtApply(version, enabled)`——扩展是「容器内编译 → 固化镜像 → 重建」另一条链路（AGENTS.md §5.16.2）。默认预勾选 = `commonExts(version)` 的 11 项常用；一项不选即只装基座镜像、不起第二个任务。
- **扩展目录是前端唯一清单**：`frontend/src/constants/ext.ts` 的 `EXT_CATALOG`（**73** 项 · **8** 分组），`InstallModal` 与 `PhpExtensionsModal` 共用；其中 `tool: 'pecl'` 的项必须与后端 `internal/config/extensions.go` 的 `peclExts` **逐项相等**——分类错了就是「用内置命令装第三方扩展」，必然编译失败，故由第 5 项门禁 `scripts/check-ext-catalog.go` 对账（§5.16.1）。目录是**便利集不是白名单**：手输任何过 `ValidateExt` 的名字都能装（最小限制原则）。
- `ExtStatus(ctx, version) (model.ExtStatus, error)`：**打开「管理扩展」弹窗时由后端到容器里现查一次「此刻哪些扩展开着」**（总纲 §0.2 规则 37 · §5.16.2，v2.9.14 追加；已落地，取代原 `ExtList`）。它跑两条**只读 argv 探针**（`php -m` 给「开着哪些」、`ls -1 conf.d` 给「哪些删得掉」，都**不经 shell**）→ 后端把显示名**归一**到目录名（大小写 `PDO`→`pdo`、`SQLite3`→`sqlite3`，以及 `Zend OPcache`→`opcache`，一处收口在 `config.NormalizeExtName`）→ **与库里不同才**回写 `php_extensions` 并广播快照，相同则一写都不发（每次开弹窗都重发同一份事实等于刷屏，§5.19.4 同口径）。返回体是 `model.ExtStatus{Version, Enabled, BuiltIn, Live}`；**弹窗的勾选仍取快照** `phpExtensions[version]` 那一份（硬红线 4：前端**不**直接消费 `php -m` 文本、**不**新增事件名），**提交时仍是全量目标集**。
  - **容器未运行 / 实测失败 / 实测为空**：三种情况一律退回库里那份并把 `Live=false` 带回去（界面据此显示一行「非实时」、期间不可提交）；退回时**不写库、不发事件**——伪造一份没人实测过的权威值比显示旧值更糟，也**绝不**因为拿不到实测就把整片返回成空（那等于把「不知道」说成「没有」）。探针报错不得静默当作「一个都没启用」（与 §5.14.12「`ImageExists` 报错不上抛即误判」同口径）。
  - **可停用性随数据一并给出**（三档显示，§5.16.2 / §5.16.4）：`BuiltIn` 就是「在实测集里、但 `conf.d` 里没有那份 ini」的名字——静态编进基座的扩展（真机干净基座 **19** 项）无 `.so`、无 ini，`rm -f` 对它是空操作，界面必须显示 `on · 内建不可停用` 而**不是**一颗可取消的开关；只有 `opcache`／`sodium` 这类有 ini 的才属 `on · 可停用`。
  - **`Live` 与 `BuiltIn` 不进快照**（只有一个权威）：它们随本次请求/响应回去，只用来决定「这一行能不能点」；启用集本身仍只从快照读。方法名换了但**一进一出**，`app.go` 导出方法仍 **70**、生成的绑定仍 **68**（= 70 − `ServiceStartup`/`ServiceShutdown` 两颗生命周期钩子），§4.1／§11.3 的口径不变。
- `ExtApply(ctx, version, enabled)`：**入参是「目标扩展集」而非增量**——容器内编译 → commit 固化 `phpo/php:{version}` → **提升离线缓存 → 清空临时目录**（§5.14）→ 重建容器 → 重载 nginx。启用集与当前一致时直接返回，不产生任务也不重建。
  - **落库的必须是实测集，不是入参**（总纲 §0.2 规则 37）：任务结尾写 `php_extensions` 之前要再取一次 `php -m`——目标集里可能有编译失败的项、而基座内建的项从来不在目标集里；把入参原样落库就是界面常年说谎的根源（§5.16.2）。`removed` 里若混进没有 ini 可删的名字（基座内建、或根本不存在的那些），后端**跳过并聚合成一行 `dim`**（「基座内建（无 conf.d ini 可删），停用无效，已跳过: …」），不给它们跑空操作、也不因此判死整单。
  - **停用扩展 = 删 ini**：镜像里没有 `docker-php-ext-disable`（真机在 `php:8.4-fpm` 里 `ls /usr/local/bin` 只有 `docker-php-ext-{entrypoint,configure,enable,install}` 与 `docker-php-source`），所以停用的动作是 `rm -f /usr/local/etc/php/conf.d/docker-php-ext-<name>.ini`（§5.16.4）。
  - **固化镜像提升到的是第二槽位**（总纲 §5.14.2 / v2.9.14 需求 ③）：`cache.PromoteExtImage(version, ref, tmpTar)` 落 `config.Env.OfflineExtImageTar("php", ver)`＝缓存根下 `php/{ver}/image-extensions.tar`，清单字段 `manifest.extensions_image`——**与基座的 `image.tar` / `image` 各占一份，绝不互相覆盖**（同一版本两份镜像挤一个槽位，后提升者会把前一份的 SHA256 记录改掉，下次装必判损坏）。提升成功必须**一行 `ok` 点名落点全路径**（`已提升到离线缓存: phpo/php:8.4 → …/image-extensions.tar`），否则用户无法确认「扩展装进缓存了没有」。
  - **弹窗生命周期**（v2.9.14 需求 ⑥，**覆盖** v2.9.9「失败时弹窗不关」口径）：`App.ExtApply` 是**同步执行**的数十秒级链路，故 `PhpExtensionsModal.apply()` **提交即 `emit('close')`**——等待期的步骤、进度与失败全部由抽屉日志与右栏队列承载（§5.6.4 同一路子），`.then` 里 `await syncState()` 后 toast 成功、`.catch` toast 后端原文。重开弹窗的默认勾选**是**该版本容器内**此刻已启用**的扩展集——即上一行 `ExtStatus` 实测 `php -m` 归一后回写、再随快照回流的那一份（`appState.phpExtensions[version]`，**不得**是空清单，也**不得**是「上次 Apply 请求的目标集」：目标集里可能有编译失败项，而基座内建的 `curl`／`mbstring`／`openssl` 从来不在任何目标集里，照旧读入参即界面说谎，§0.2 规则 37）。
  - **包本体离线化：安装与重新编译都必须做**（总纲 v2.9.14 追加 / §0.2 规则 36 / §5.14.3）——缓存的对象是 **`{name}-{版本}.tgz` 与 Alpine 的 `.apk` 文件本身**，不是扩展名，也不是只有 `.so` / 固化镜像那两份「结果」。
    - **pecl**：`cache.LookupExtPackage(version, "pecl", name)` 按 `{name}-` **前缀**匹配（精确名带版本永远查不到，等于每次白拨网络）、多份取版本序最大；命中即 `engine.CopyTo` 回填容器 `/tmp/phpo-ext/pecl/` → `pecl install <该文件>`（零网络，发 `cache:hit{source:offline}`）。未命中发 `cache:miss{action:download}` → `pecl download <name>` 落在暂存目录 → `engine.CopyFrom` 取回宿主 `./php/{ver}/ext/pecl/` → `PromoteExtension`（算 SHA256 + 写 `manifest.pecl`）→ 再从文件编译。
    - **apk（构建依赖）**：先 `config.ExtPkgProbeCmd` 探基座包管理器（判据 `/lib/apk/db/installed`——官方 `docker-php-ext-install` 自己也是看这一个文件决定要不要 `apk add`，跟着它才不会判成两套）。`apk` 族才做：`ListExtPackages` 把既有 `.apk` 零网络回填 → `config.ExtApkPrefetchCmd()` 用 **`--cache-dir /tmp/phpo-ext/apk`**（不是官方脚本的 `--no-cache`——用完即弃就永远拿不到可离线文件）+ `--virtual .phpo-ext-deps $PHPIZE_DEPS` 预取 → 取回宿主 → 提升 `manifest.apk`。`deb` / `none` 基座一行 `dim` 说明「不产生可离线包文件」，**不造槽位、不判死整单**。
    - **清理时机两条**：`rm -rf /tmp/phpo-ext`（`ExtStagingCleanupCmd`）**固定在 `docker commit` 之前**，否则包文件被固化进 `phpo/php:{version}` 镜像层；宿主临时目录 `./php/{ver}/ext/` 由 `Apply` 的 `defer ClearTempDir` 覆盖整条链路（含固化镜像导出的落点），成败/取消共用一次、只发一次 `cache:tempdir-cleared`。
    - **降级口径**：缓存查询·损坏·回填·取包·取回·提升**任一环节**失败只 `dim` 一行并退回在线编译，绝不判死整单（§0.2 规则 16）。宿主⇄容器唯一字节通道是 `engine/copy.go` 的 `CopyTo`/`CopyFrom`（Docker archive API；php 挂载表没有 `ext/` 这一档）。用例：`internal/cache/extpkg_test.go`（前缀命中取最大版本 / 损坏与目录缺席 / 提升登记）+ `internal/service/extension_service_test.go` 的 `TestExtension_Apply_PeclCacheHit_ZeroNetwork`、`_ApkPrefetchOnAlpine`、`_DebBaseSkipsApkPrefetch`、`_CacheFailuresDegrade`、`_ExtPkgCorrupted`。
  - **后置环节不得判死已做对的工作**：「重载 Nginx」一步四档 `dim`（`reload` 未接入 / nginx 未安装 / 运行态探针报错 / nginx 未运行），重载**失败**只一行 `err` 后 `return nil`——**不撤回本次已编译生效的扩展**（§5.16.3）。
  - **执行期日志是契约的一部分**（§5.16.3）：六个步骤名固定，容器内编译的 stdout/stderr 经 `engine.ExecStream`（`stdcopy` 去 8 字节帧头）**逐行**发 `task:log`；某一项失败时错误消息必须**点名该扩展**并说明「本次扩展集未应用」，前端原样 toast。
  - **php 的装配镜像按本机实际态现取**（`LifecycleService.specFor`，安装与重建共用）：`ImageExists(phpo/php:{version})` 为真就用固化镜像，本机没有则先让 `cache.LoadExtImage(ctx, version)` 从第二槽位**零网络 `docker load` 恢复**（命中发 `cache:hit`，SHA256 不符发 `cache:corrupted` 后回落基座），两条都拿不到才用基座 `php:{version}-fpm`。若一律用基座，装/重建一次 php 就把用户已启用的扩展静默抹掉，而 `php_extensions` 表还记着它们（违反 §5.13.1 一致性）。探针报错**必须上抛**，静默当成「本机没有」等于把这次抹除伪装成正常重建（§5.14.12 禁止静默）。
  - **编译失败的回滚目标同样现取**：原启用集非空且固化镜像真在本机才回滚到固化镜像，镜像已被手工删除/换机没带过来则退回基座——否则回滚的第一步建容器就 `No such image`，用户连「重新点一次扩展」这条恢复路都没有（§5.13.1 可恢复性）。
  - **退回基座即全量重编译**（总纲 v2.9.10 / §5.16.2）：基座镜像**不含任何**原启用扩展，故该分支的待编译集换成**完整目标集**（`added = 去重排序后的 enabled`、`removed` 置空——基座本就不含那几项，无需再删 ini）。否则 `prev` 里那几项既没被编译、又会在任务结尾被原样写进 `php_extensions` 并随快照广播，界面显示「已启用」而 `php -m` 里没有（违反 §5.13.1 一致性）。口径变了必须写进日志：一行 `dim`「固化镜像 {ref} 不在本机，容器上无原扩展集，目标扩展集全量重编译」。用例 `TestExtension_Apply_BaseFallbackRecompilesFullSet`。

### 6.2 备份
- `BackupList()` / `BackupPath(file)` / `BackupSaveAs(file, dst)`：列表与导出（`file` 仅接受合法文件名，防穿越）。
- `BackupCreate(ctx)`：**逻辑导出数据服务 → 暂停数据服务 → 导出 SQLite 快照 → 打包 → 自动重启**。第一步在暂停**之前**执行，因为 `mysqldump` / `pg_dumpall` / `redis-cli --rdb` 要连着活的实例（§5.17.2）。
  - **导出逐 kind 独立容错**：某个版本未运行只记一行 `dim` 日志；转储失败记 `err` 并继续（「本次归档不含该库数据」），**不判死整包**；空转储文件删除、半途失败的残留文件回收。转储走 `engine.ExecStream` 去帧逐行进日志，stderr 不入转储、只留作报错原因。
  - **打包读不动即跳过**：`archive.Create` 返回 `([]string, []Skip, error)`——单个条目 `open` 失败不再让整次备份报错，改为跳过并汇总；`backup_service.logSkips` 按所在目录聚合（每目录至多列 5 项，另起一行报总数）逐行告警。只有写 tar 自身失败才上抛（§5.17.1）。数据完整性靠上一步的逻辑导出补回。
- `BackupRestore(ctx, file)`：清空命名空间 → 解包落盘 → 逻辑重放 SQLite → 重建容器。**归档里的 `dump/` 随包携带但恢复不自动重放**（重放别人的转储等于悄悄覆盖目标机数据）——任务日志会明示这一点，需要恢复库内数据由用户自行执行（§5.17.3）。
- `BackupDelete(ctx, file)`：删除归档（二次确认在前端）。

### 6.3 doctor
- `DoctorRun(ctx) (DoctorReport, error)`：§5.7 的 15 项检查 + 汇总计数。
- `DoctorFix(ctx, id)`：一键修复（`calibrate` / `clear_temp`），幂等可重复。

### 6.4 升级
- `UpdateCurrentVersion() string`、`UpdateCheck(ctx) (UpdateAvailable, bool, error)`。
  检测把 `config.yaml` 的 `update_sources` 数组**全部并发**探测（每源独立 15s 超时，超时即判该源不可用，不拖累其余源），在成功的源里取**版本序最新**的一份——镜像源常滞后于原始发布，「先命中即返」会把「检测最后发布的版本」做成「第一个可用源宣称的版本」；版本并列时取数组靠前的那份，数组顺序因此只决定并列裁决与报错点名的次序。全部失败才逐源点名报错（此时总等待 ≈15s 而非 N×15s）。命中源的名字进载荷 `source`，清单的 `download_page` 原样透传给「打开下载页」。清单内 `assets` 按本机 `os`（Linux 再分 `deb`/`rpm`）选出要装的那一颗。
- `UpdateApply(ctx)`：经任务引擎串行执行（下载 → **SHA256+Ed25519 双校验** → 备份 → 安装，失败回滚，硬红线 5/6）；进度走 `update:progress/done`；前面有任务则排队，同标签重复提交回 `ErrQueued`。

### 6.5 清理与回收站
- `CleanupScan(ctx)`：全量孤儿扫描并广播 `docker:orphan-found`。
- `CleanupRun(ctx, mode)`：三模式删除孤儿；激进涉卷删除由前端二次确认。
- `CleanupCache(ctx, mode)`：三模式清理离线缓存（在用受保护）。
- `TrashList` / `TrashRestore(ctx, id)` / `TrashEmptyExpired(ctx)`：7 天回收站。
- `OperationList(limit)`：最近审计历史（文件权威见 `operations.log`）。

### 6.6 离线缓存（§5.14.10 全量 API）
`OfflineListEntries` / `OfflineGetEntry` / `OfflineStats` / `OfflineVerifyEntry` / `OfflineVerifyAll` / `OfflineCleanupCache` / `OfflineRemoveEntry` / `OfflineLookupImage` / `OfflineLookupExtension` / `OfflinePromoteImage` / `OfflinePromoteExtension` / `OfflineClearTempDir`。
- 命中前一律先校验 SHA256；损坏发射 `cache:corrupted`。
- `OfflineClearTempDir(ctx, kind, version, reason)` 清空临时目录并发射 `cache:tempdir-cleared`。
- **扩展固化镜像的第二槽位不经此门面**（v2.9.14）：`cache.Manager` 的 `LookupExtImage` / `LoadExtImage` / `PromoteExtImage` 只服务装配链路（`specFor` 零网络恢复、`promoteCommitted` 提升），**不面向界面**——UI 侧只见 `CacheEntry.hasExtImage`（`manifest.extensions_image` 是否已缓存）。门面不加方法即绑定方法数不变，也不新增事件名（复用 `cache:hit` / `cache:corrupted` / `cache:promote`）。

### 6.7 装机向导
- `HomeVerify(ctx, home, www)`：**只读预检**——校验路径安全、判存在、按权限位判可写；**不创建任何目录、不写探测文件、不落库、不发事件**。尚不存在但最近已存在祖先可写的条目报「○ …（确认后将创建）」。
- `HomeEnsure(ctx, home, www)`：**目录与文件的唯一创建入口**（用户点「确认并创建」才触发）——先决检测 `Configured()`：两根「已持久化 + 目录实际存在」即为已设置，一律跳过创建、不改写 `config.yaml`（即便本次请求路径与已设目录不同，也不建第二套工作目录），仅广播权威快照令 UI 归位；未设置才走三段式创建工作目录子树（`MkdirAll` + 试写探测）并把两根目录落地 `config.yaml`，随后广播 `state:changed`（`dirReady` 不落库，由快照按「两根已持久化 + 目录实际存在」派生；两根一旦落地即解锁运行态库的延迟创建）。
- `HomeDefaults()`：返回 `PHPO_HOME` / `WWW_ROOT` 预填值（`config.yaml` 已存根 > `~/phpo` 默认）+ `CONFIGURED`（`"true"`/`"false"`）——向导挂载即读，`CONFIGURED=true` 时只回显已设两根与「完成」，不进入三步设置流程。
- `Restart()`：重建对象图的唯一手段——各服务在装配期按值持有展开后的两根（`internal/app/di.go`），会话中途改根无法靠刷新快照归位，只能按 `config.yaml` 重启重建。置 `restartPending` 后 `Quit()`，实际拉起在 `ServiceShutdown` 释放资源后由 `relaunchSelf()` 以子进程完成，并带环境标记 `PHPO_RESTARTED=1`；带标记的第二代再被请求重启即拒绝报错，防「刷新失败→重启」无限循环。
- 刷新优先、重启兜底：前端 `useStateSync.ts#syncState()` 是「主动拉取权威快照」的公共入口（启动、手动同步、装机向导完成/已设置态四处复用，与 `state:changed` 走同一 `applySnapshot`，硬红线 4）。工作目录设置完成或检测到已设置时**第一件事**就是 `syncState()` 令主界面与弹框同一时刻归位；同步后 `homeReady` 仍为 false 才升级请求 `Restart()`。
- 判据统一：`Configured()` 与快照 `dirReady` 双 true 同源（`ConfigStore.RootsReady()`），前端 `homeReady` 与后端拦截共用一套标准，不另立判据。

### 6.8 Docker 镜像源（设置页：多源 · 测速 · 拉取优先最快）

这一组管的是「装镜像时要联网拉取，从哪台服务器拉」。三个方法都在 `app.go`，前端走 `frontend/src/api/dockerSource.ts`。

- `DockerSourcesGet() ([]string, error)`：读回已保存的镜像源清单（顺序即 `config.yaml` 里 `docker_sources` 的书写顺序）。**空清单＝不改写，直连官方**。设置页挂载即读一次做回显——权威值在 `config.yaml`，界面不回填本地输入框（硬红线 4）。
- `DockerSourcesSet(ctx, hosts []string) error`：保存清单（每行一个「主机[:端口]」，空行忽略）。先过 `docker-source-set` 裁决：任一非法项即**整单报错并点名原值**，不静默丢掉那一行；清单为空只出警告（「将直连官方」）。保存即对**下一次拉取**生效——不需要重启、不需要重建容器。这里**不测速也不拦「连不上」**：选了暂时连不上的源照常收下，拉取时逐项点名后自动回落直连官方（最小限制：能警告的不要阻止）。
- `DockerSourcesProbe(ctx, hosts []string) ([]model.MirrorSource, error)`：并发测速界面上填的这几行地址，返回顺序与入参**逐行对应**。`MirrorSource{ host, latencyMs, ok, error }`——`latencyMs` 是 registry v2 握手（`GET /v2/`）的往返毫秒数，**不是下载带宽**；`ok=true` 表示这台能握手（HTTP 200/401/403 都算通，401 只是要登录），`error` 缺席即没问题。**只读**：不落库、不建任务、不进账本，结果按请求/响应直接回给界面，因此**无需事件名**（§5.6 的 17 个冻结口径一个未增、快照字段一个未加）。

**它不是第四个「根」**（与 §5.15 三根的唯一区别）：镜像源是主机名不是路径，不参与 `rootsKey` 指纹（`internal/app/di.go`），因此**不换图、不 `Rebind`、也不受「队列非空即 `TaskBusy`」那条门禁约束**。缓存管理器拿的是指向对象图那份 `config.yaml` 的**懒读 provider**（`cacheMgr.SetSourcesProvider(cfg.DockerSources)`），所以保存必须写同一份实例（`Container.GraphConfig()`），下次拉取现读——改源与改根是两条不同的生效链路，别混。

**生效范围只有一步**：决策 22 的优先级「离线缓存 > 本机 Docker 镜像库 > 网络」一字未改，镜像源**只参与 `docker pull` 那一级**（`cache:miss` 的 `action=pull`）。命中缓存与「本机已有镜像 `docker save` 重建缓存」两条零网络路径**不查源**；扩展包联网下载（`action=download`）走的是 pecl / Alpine 官方仓库，也不查源。用哪一台拉成功，记在 `cache:miss` 载荷新增的 `source` 字段里（空即直连官方）。

## 7. preflight 契约（唯一裁决层）

所有 action 在执行前先过 `internal/preflight/preflight()`；UI 层即时校验不替代它（§0.2-14）。

**20 个 action（§0.3 权威值：原型 17 + 生产新增 3）**，分四组：

| 组 | action |
|----|--------|
| service（6） | `install` `uninstall` `service-stop` `service-start` `update-config` `service-config` |
| site（6） | `site-add` `site-remove` `site-port` `site-vhost` `php-switch` `rewrite` |
| ops/cache（5） | `extensions` `backup` `restore` `backup-delete` `offline-prune` |
| 生产新增（3） | `root-set`（改缓存根／备份根／数据目录，需求 1/7/8）· `cache-import`（手工导入缓存条目，需求 1）· `docker-source-set`（保存 Docker 镜像源清单，总纲 §5.21） |

- `ActionCount = 20`、`NeedsHomeCount = 18` 为对账测试常量（对账用例 `TestActionAndNeedsHomeCounts`）。
- **NEEDS_HOME（18）**：除 `php-switch`、`backup-delete` 外的 action 需 `PHPO_HOME`（`dirReady`）就绪，否则返回 `NEEDS_HOME`（引导装机向导）。新增三项入内的理由：**`root-set` 写 `config.yaml`**——两根未就绪即拒绝，否则会在用户数据目录凭空建出配置文件（违反「首启零落盘」）；**`cache-import` 写缓存根**——与 `offline-prune` 同组；**`docker-source-set` 同样写 `config.yaml`**——与 `root-set` 同一条理由（它写的是一节清单而非某个根，但落盘对象同一份文件）。
- `root-set` 的裁决在 `rules_root.go#rootSet`：路径仅做路径安全校验（§5.4 唯一保留的校验），`field=data_dir` 时必须有 kind/version 判据，服务运行中只出 `warn`（改后需重建容器才生效），不阻断。
- `cache-import` 的裁决在 `rules_cache.go#cacheImport`：`field ∈ {image, apk, pecl}`，且 `apk`/`pecl` 仅限 `kind=php`；源文件不存在由 `errs.FileMissing` 兜住。
- `docker-source-set` 的裁决在 `rules_docker.go#dockerSourceSet`：整批清单交 `config.ValidateRegistryHosts`（一行一个「主机[:端口]」，去重保序），**任一非法项即整单报错并点名原值**（半套清单落盘比不落地更让人困惑）；清单为空只出 `warn`「未填写镜像源，拉取将直连官方」——**不阻断**（能警告的不要阻止，§0.2-16）。**不测速、不因「此刻连不上」而拒**：源是否可用属运行时事实，保存时拦等于替用户决定「这台以后也不要用」。
- **`uninstall` 没有「至少保留一个」门禁**（总纲 §1.11 / v2.9.14 需求 ⑤）：`rules_service.go#uninstall` 只剩 `SvcMissing`、`NotInstalled` 两条 `errf`；php 有依赖站点、php 是最后一个版本、nginx 有依赖站点——三处一律 `warnf` 后**照常卸载**。原型 `PF.lastPhp`「至少保留一个 PHP 版本」这一码已随之删除（`pkg/errs` 码数 **28 → 27**，以 `CodeCount` 为准）。用例 `TestUninstallDependentSitesWarnNotBlocks`、`TestUninstallLastPhpAllowed`、`TestUninstallNginxDependentSitesWarnNotBlocks`。
- 校验遵循最小限制：能警告不阻止；除 8 条硬红线外一律放行（§0.2-16、§3.3）。

## 8. 返回值约定

- 错误经 `pkg/errs` 包装，携带可 i18n 的 code（如 `portInUse`、`TASK_BUSY`、`NEEDS_HOME`）；`hint` 为人话文案（§3.2 原则 3）。
- 写方法返回 `error`；查询方法返回 `(T, error)`；事件载荷见 `docs/事件流协议.md`。

## 相关文档

- [领域模型](./领域模型.md)
- [事件流协议](./事件流协议.md)
- [架构设计](./架构设计.md)
- [状态同步](./状态同步.md)
