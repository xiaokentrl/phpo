# 密码策略

> 对应 AGENTS.md §1.5 / §5.2 / §0.2-5/6 / 工单 T704。密码为本项目**明文策略**，禁止任何加密/复杂度/长度校验。冲突以 AGENTS.md 为准。

## 1. 六项规则（§1.5，唯一权威）

| # | 规则 | 说明 |
|---|------|------|
| 1 | 明文存储 | 存于 `~/phpo/.env` 与 SQLite `env` 表 |
| 2 | 默认 `123456` | 新建服务时自动填入（`config.DefaultPassword`） |
| 3 | 可修改 | UI 行内编辑 |
| 4 | 可为空 | 空字符串是合法密码 |
| 5 | 长度不校验 | 1 位、100 位、含特殊字符、含空格均合法 |
| 6 | UI 可查看 | 👁 显示 / 📋 复制 / ↺ 重置 |

## 2. 明确禁止（§0.2-5/6、§1.5）

- ❌ 校验密码长度、复杂度、非空。
- ❌ 引入 keyring、Vault、AES、bcrypt、argon2 等任何形式的加密或哈希。
- ❌ 后端对密码做任何规范化（trim 之外不改写大小写/去空格）。

## 3. 落地

- 常量：`internal/config/password.go` → `DefaultPassword = "123456"`；`GenPassword()` 无随机含义，按策略回落默认。
- 读写：`internal/service/env_service.go` 的 `GetPassword` / `SetPassword`（`app.go` 暴露为 `GetServicePassword` / `SetServicePassword`）。
  - `Get` 未设置时回落 `123456`；`Set` 明文写入，**零校验**，写后广播 `state:changed`（硬红线 4）。
- 存储：`internal/store/password.go` + SQLite `env` 表；快照序列化于 `store/snapshot.go` 的 `env` 字段。
- UI：`PasswordField.vue`（👁/📋/↺）；空串通过即时校验，不被前端拦截。

## 4. 与其它策略的关系

- 数据服务（mysql/pgsql/redis）root/默认用户密码走同一明文策略（M5，`db_service.go` / `redis_service.go`）。
- 备份包含 `env` 明文 → 属敏感数据，脱敏与导出边界见 `docs/备份脱敏规范.md`。
- `.env` 文件权限：mac/linux `chmod 600`，Windows NTFS ACL（仅文件系统权限，非内容加密，§8）。

## 相关文档

- [版本策略](./版本策略.md)
- [备份脱敏规范](./备份脱敏规范.md)
- [跨平台差异](./跨平台差异.md)
- [接口契约](./接口契约.md)
