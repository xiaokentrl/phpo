# phpo 环境占位示例（真实值运行时写入 ~/.phpo/config.json 与 ~/phpo/.env，此处仅开发者本地覆盖）
# PHPO_HOME：数据根目录（服务配置/日志/离线缓存），默认 ~/phpo
PHPO_HOME=~/phpo
# WWW_ROOT：站点根目录，默认 ~/www
WWW_ROOT=~/www
# 数据库密码策略：明文，默认 123456，可为空，长度不校验（总纲 §1.5）
PHPO_DEFAULT_PASSWORD=123456
