# phpo 任务工单（逐阶段展开）

> **文档类型**：实施工单清单（派生文档，从属 `AGENTS.md` 总纲 v2.9.3 与 `实施顺序.md`）
> **编号规则**：`T<阶段><序号>`；验收标准全部可执行（§3.4.4 目标驱动：先写用例，调试至通过）
> **字段约定**：依赖 = 前置工单；交付 = 涉及文件（对齐总纲 §4.1 目录树）；验收 = 成功标准（缺测试用例不得声称完成）
> **冲突裁决**：本文与 AGENTS.md 冲突时，一律以 AGENTS.md 为准

---

## 阶段 M1 · 骨架

**T101 · 后端 Wails3 骨架** ｜ 依赖：—
- 交付：`go.mod`（module phpo / go 1.27）、`go.sum`、`wails.json`、`Taskfile.yml`、`Makefile`、`main.go`、`app.go`、`.gitignore` `.editorconfig` `.golangci.yml` `.air.toml` `config.example.yaml`
- 要点：仅 GUI 入口，无 `cmd/` 目录（§5.1）；开发期热重载用 `task dev`（`wails3` beta 的 `dev` 参数与本机不匹配，不作为验收前置）
- 验收：本机 `wails3 build`/`task build` 出 Linux 二进制并可起空窗口；`CGO_ENABLED=0` 交叉编译在 CI/三平台打包工单（T701/T703）验证

**T102 · 前端工程骨架** ｜ 依赖：T101
- 交付：`frontend/package.json` `vite.config.ts` `tsconfig*.json` `index.html` `src/main.ts` `App.vue` `src/router/index.ts`
- 要点：Vue 3.5+ / TS 5.x / Pinia 2.x；10 路由对齐原型 `NAV`（sites/php/mysql/pgsql/redis/nginx/backup/offline/settings/overview）；`frontend/bindings/` 由 `wails3 generate bindings` 生成、禁止手改（§0.2 规则 13）
- 验收：10 路由全可达；`pnpm build` 无 TS 报错

**T103 · i18n 提取（前端）** ｜ 依赖：T102
- 交付：`frontend/src/locales/zh-CN.ts` `en-US.ts`
- 要点：从原型 `MESSAGES`（约 300+ 键）全量直译提取，键名不变；语言切换即时重渲染
- 验收：键数对齐脚本比对（zh-CN 与 en-US 键集合相等）；切换语言无缺键

**T104 · 主题系统** ｜ 依赖：T103
- 交付：`frontend/src/styles/themes/*.css`（6 套）、`stores/prefsStore.ts`
- 要点：`THEMES`(6) 全量迁移；偏好仅存 localStorage（§3.1 原则 5）
- 验收：6 主题逐屏对照原型截图无回归；重启后主题保留

**T105 · 布局与缩放** ｜ 依赖：T102
- 交付：`stores/layoutStore.ts`、`composables/useZoom.ts` `useResize.ts`
- 要点：侧栏宽/抽屉高滑杆 + 3 预设 + 重置；`UI_SCALE.snap` 7 档缩放与 resize 补偿；命令面板 layout/zoom 命令联动
- 验收：各档位缩放下模态拖拽 resize 无错位（对照原型 zoom 补偿逻辑）

**T106 · 视图迁移 ×11** ｜ 依赖：T104、T105
- 交付：`views/` 12 个 .vue = 11 路由视图（含 CleanupView、OfflineView）+ `ServiceView.vue`（服务页共用基座）
- 要点：M1 阶段数据源为 mock state；表格/卡片/行内编辑结构与原型 1:1
- 验收：逐屏 diff 清单全过；空态/异常态样式齐全

**T107 · 模态迁移 ×12** ｜ 依赖：T106
- 交付：`components/business/` 各 Modal（安装、建站、站点配置、伪静态、扩展、配置、危险确认、装机向导、主题、升级、清理、缓存详情/缓存清理等）+ `TrashViewer.vue` `DockerGate.vue` 壳；端口无独立模态（行内编辑走 `usePortSuggest`）、备份/恢复在 `BackupView` 内联
- 要点：模态外壳统一（可拖拽/resize/遮罩关闭）；M6 才接真实逻辑的仅做壳
- 验收：12 模态均可打开、表单即时校验行为与原型一致
- **2026-09-22 修正（三根可自定义带来两件新前端交付，`common/` 7→8、`business/` 18→19）**：
  - **`components/common/EditablePathBar.vue`（第 8 件）**——缓存根 / 备份根 / 每服务版本数据目录**共用的可编辑路径条**，与只读的 `PathInfoBar` 分立（原型那条纯展示，不承载写链路）。四个动作：手输绝对路径、「浏览」经 `Dialogs.OpenFile{CanChooseDirectories:true}` 原生选目录（`hasBackend()` 为假时不出按钮，demo 通道只留手输）、保存、「恢复默认」。回显口径：`path`/`default-path` 两个 prop 都取快照 `env`，组件内 `val` 只随 `watch(() => props.path)` 更新——**保存后不自行改写输入框**，等 `state:changed` 把它带回来（硬红线 4）。三处消费方：`OfflineView`、`BackupView`、`ServiceView` 的数据目录行。
  - **`components/business/CacheImportModal.vue`（第 19 件）**——手工把**任意已有文件**登记成一条离线缓存（需求 1「读写任意缓存文件」）。三个字段：服务（5 种）、版本（候选取「已装 ∪ `SVC_META.suggested`」，仅作提示、不限制字符集 §1.6）、落点（`image` → `{kind}/{version}/image.tar`，与在线安装**同一条目**；`apk`/`pecl` 仅 php 可选）。走 `preflight('cache-import', …)` → `useCache().doImport` → 后端任务 → `cache:promote` 回流，列表由写后补刷，**前端不本地插行**。

**T108 · 命令面板** ｜ 依赖：T106
- 交付：`components/common/CmdPalette.vue`、`constants/cmd.ts`
- 要点：`CMD_ITEMS`(20) 全量；⌘1–9 路由跳转；layout/zoom 命令可用
- 验收：20 命令逐项触发验证；模糊搜索命中正确

**T109 · 日志抽屉（任务引擎 mock）** ｜ 依赖：T102
- 交付：`components/business/TaskDrawer.vue`、`stores/taskStore.ts`、`composables/useTask.ts`
- 要点：日志行 5 类型（cmd/meta/ok/dim/err）；任务状态 4 态（running/success/failed/cancelled）；**全界面不展示等效命令**（见下方 2026-09-23 修正条；本条原口径「『等效命令』标注『仅展示』+ 复制时 Toast『本产品不提供 CLI』」已被 AGENTS.md §1.4 v2.9.11 撤销）
- 验收：mock 脚本逐行回放、取消按钮生效；**真实宿主下并发写不阻塞**——`task.Manager` 串行 FIFO，前一个任务在跑时再发起的写操作进队列（队列详情随 `state:changed` 的 `Snapshot.Tasks` 推送，排队项可 `CancelQueued` 撤回），同标签重复提交由后端 `ErrQueued` 当场拒绝；`preflight` 不再有 taskBusy 全局守卫，忙锁文案（taskBusy）只保留在无宿主的 demo 通道与托盘退出守卫
- **2026-09-22 修正（左 70% 日志 / 右 30% 队列，新任务置顶，提交即上屏）**：抽屉改为两栏——`.drawer-main`（`flex:0 0 70%`：进度条 / 失败原因 / 日志流）与 `aside.drawer-queue`（`flex:0 0 30%`，`.dq-list` 独立滚动）。队列是**竖排行**（状态点 · 标签 · `step/total` · 显示态文字 · 撤回按钮），**新提交永远在最上面**，行位只随提交先后决定，运行中任务不因取得执行权而下移。每行文字状态由 `displayOf` 从权威快照分区 + `task:done` 终态**派生**（等待中/执行中/已完成/失败/已取消 + `unknown` 预留兜底，新增档只改 `DISPLAY_LABEL` 与 `displayOf`）；`syncBoard` 为每个 `Pending` 项建记录，故一经提交立即出现在列表、不等执行权；撤回后行随快照消失，被撤回项若正被选中则选中回落运行中任务。详见 AGENTS.md §5.6.1（**无新事件名、无第 5 个任务状态、零后端改动**）
- **2026-09-22 修正（抽屉三改：头部标签固定「服务」／两栏可拖拽／系统日志通道，AGENTS.md §5.6.1 §5.6.2）**：① **需求 5**——`.drawer-head` 左侧标签原随当前路由变（站点页显示「站点」…），现恒为 `t('nav.services')`「服务」二字（复用既有键，**无新文案键**）。② **需求 4**——§5.6.1 的 70/30 由「固定占比」改口为**默认占比**：`.drawer-splitter`（7px、`col-resize`）按住 `pointerdown` 可左右拖，占比经 `layoutStore.setSplit()` 夹在 **40–80**（`LAYOUT_LIMITS.split`），写入 CSS 变量 `--drawer-split`，存 `localStorage`（`phpo-drawer-split`，§3.1 原则 5 UI 偏好不落库），**双击中缝复位 70**。「以后可能加别的状态要提前预留」与「布局可让给用户」两件事就此分开：显示态仍只有那一张映射表，占比只是默认值。③ **需求 3/6**——`OfflineView` 底部的 `.off-events` 小面板（「暂无事件；安装服务或扩展时…」）删除：它与 §5.6 事件协议脱节、且把缓存事件关在单个页面里。改为抽屉的**系统日志通道**：`taskStore.sysLines`（上限 `SYS_MAX=200` 裁尾）承接**无任务归属**的事件逐行流水，`eventLine()` 按 `app.tasks.running?.id` 分流——有运行中任务就归该任务（串行队列 ⇒ 归属唯一），没有才进系统日志；左栏无选中任务时显示 `sysLines`，空态文案 `task.sysEmpty`。**这不是第 5 个任务状态**：不为事件建 `TaskRecord`/`TaskBrief`、不写 `operations` 账本、**无新事件名**（仍 17），唯一显式豁免是 `update:progress`（连续量，由升级弹窗进度条承载）。
- **2026-09-23 修正（全界面不再展示等效命令；三区口径写进总纲，AGENTS.md §1.4 §5.6.1 / 总纲 v2.9.11）**：抽屉头部原有一条常驻文本 `phpo site remove demo.test` + 「仅展示」徽标，点它弹 Toast「本产品不提供 CLI」。这与本产品立场自相矛盾——**仅 GUI、不提供 CLI**（§2.1、§0.2 规则 4），却在唯一常驻的日志出口摆一条形态可执行、还能复制的伪命令行，徽标只是用文案去补一个本不该存在的设计缺口。而且「头部只有左区固定为『服务』二字、中／右不改动」这一要求在 v2.9.8 需求 5 就已提出，当时却被落成「左区内的等效命令展示照旧」，等于把未收敛的旧口径冻结进总纲。现：① `.drawer-left` 只剩状态点 `.drawer-dot` + 标签 `.drawer-label`（恒 `t('nav.services')`），删 `cmdText` computed、`.drawer-cmd` span、「仅展示」chip 与 `onCmdClick`；② 中区 `.drawer-services`、右区（进度 chip / 状态文字 / 取消·复制·展开三按钮）**一字不动**；③ `taskStore.start(args, label, meta)` **仍登记参数**——`buildScript` 按 `args[0]` 选演示脚本、日志正文要用，只是任何界面位置都不外显（保留的是登记，不是展示）；④ **同一轮由用户裁定「统一去掉，保持一致」**：余下四处伪命令行一并删除，全界面共 **5 个来源**收口——`InstallModal`（`previewText()` 与「将执行」`.field`）、`SiteAddModal`（`phpo site add … --port --php --rewrite --root`）、`DangerConfirm`（`cliPreview` prop 整条删）+ `useModals.ts` 五处传参（`phpo {kind} uninstall/reinstall`、`phpo site remove`、`rm {BACKUP_ROOT}/{file}`、`phpo restore {BACKUP_ROOT}/{file}`）、`HomeSetupWizard`（`promptSeg()` 的 `$ phpo home ensure …` 验证日志首行）、`taskStore.buildScript`（demo 回放首行 `$ phpo …`）。**整行删除、不翻译成人话再显示**——参数已由表单字段、真实路径条（宿主/容器/vhost 路径）与 warnings 的人话文本给出。**`.cmd-preview` 样式类不等于伪命令行**：`InstallModal` 挂载表与 `RewriteModal` vhost 规则预览仍用等宽块承载**真实数据**，不在本条约束内、照旧保留。⑤ 文案键 `drawer.cmd` / `drawer.cmdOnly` / `drawer.noCli` / `install.preview` / `siteAdd.preview` 共 **5** 键从 `locales/{zh-CN,en-US}.ts` 删除（两侧键集仍相等，**608 → 603**）。**冻结原型 SSOT 与样式表不改**：`前端唯一界面来源.txt` 与派生 `index.html` 的 `.drawer-cmd` 结构、`styles/base.css` 的 `.drawer-cmd` / `.cmd-preview .prompt` / `.dir-log .prompt` 规则原样保留，按 v2.9.9 pgsql 日志段的先例记为一处生产偏离，由 §1.4 显式记载。验真：`vue-tsc` 退出 0、`pnpm build` 绿、五项门禁全过（i18n 各 **603**）；产物级两层取证——bundle 内 `$ phpo` / `将执行` / `Will run` 计数 **0**，浏览器点开「安装 PHP」「新建站点」实测 `.prompt` 计数 **0**、无「将执行」、`.cmd-preview` 恰 **1**（挂载表），抽屉 `.drawer-left` 恰 2 子节点、`innerText` 仅「服务」、`.drawer-cmd` 计数 0、页面不含「仅展示」；**真宿主 GUI 未走查**。

**T110 · 事件总线** ｜ 依赖：T101
- 交付：`internal/app/emitter.go` `di.go`、`frontend/src/api/events.ts`、`composables/useStateSync.ts`
- 要点：§5.6 事件表全量 17 个事件名先定义常量；M1 用后端 mock 定时器发射
- 验收：17 事件名收发逐一验证；前端只订阅、无本地乐观更新（硬红线 4）
- **2026-09-22 修正（真实宿主首帧不得残留占位假数据）**：`stores/appState.ts` 原来无条件铺原型占位数据（`installed`/`sites`/`backups`/`offline`/`phpExtensions`/`env` 的端口密码），真宿主首帧因此会显示一批后端根本不存在的站点与服务。现：① 占位数据只在 `demo = !hasBackend()` 下铺；② 宿主 Core 可能晚于 store 建立才注入，故 `App.vue` 在 `await waitForBackend()` 之后、`startStateSync()`/`syncState()` 之前调 `app.enterRealHost()` 一次性收掉（含 `dirReady` 回落 `false`，交权威快照重判）——顺序颠倒会连真数据一起抹掉；③ 真宿主 `env` 只留 `derivePaths` 的路径兜底键，删掉后端从不发的 `NGINX_PORT`/`NGINX_VERSION`/`*_ROOT_PASSWORD`（`applySnapshot` 只清 `configstore.go` 的 `EnvKey*` 发过的键，种下遗留键＝永久假值），mock 快照同步；④ `usePreflight` 的 `offline-prune` 在真宿主下改查 `cacheStore.entries`（后端权威），此前查恒为空的 `app.offline` 会把每个真实条目报成「缓存条目不存在」
- **2026-09-22 修正（宿主判据去掉 `invoke`）**：`api/site.ts#hasBackend` 曾把 `window._wails.invoke` 计为宿主信号，但 `@wailsio/runtime` 在**任何** DOM 里都会挂它（`dist/index.js`），纯 Vite 浏览器恒被判成「有后端」⇒ 上面②的 demo 门禁永不生效、界面显示「环境为空」、向导在无人应答的调用上抛错、每个绑定调用打向不存在的 `/wails/runtime`（404 风暴）。判据只取宿主独有字段 `flags`/`environment`（`wails/v3/internal/runtime.Core()` 注入，dev/prod 都带）。浏览器实测：demo 数据恢复、404 与 Vue 报错归零，只剩 runtime 自己的「Browser Environment Detected」提示
- **2026-09-22 修正（托盘下发失败要可见）**：`prefsStore.mirrorTray()` 此前 `void …catch{}` 静默吞错——勾选已写进 `localStorage` 并显示为「已生效」，原生托盘却没变＝界面失真。现失败弹 `settings.tray.pushFailed`（zh/en 各 1 键，i18n 仍 568 键集合相等）；无宿主时 `api/state.ts#setTrayPrefs` 先行 return，demo 不误弹
- **2026-09-22 修正（需求 6「一切必须实时同步」收口为可核对清单，AGENTS.md §5.6.2）**：原条款只说「后端唯一权威 + 前端订阅事件」，没有逐事件指出**落地处**，于是「实时同步」无法验收——`cache:*` 六类事件长期只进 `OfflineView` 底部的 `.off-events` 小面板，离开那个页面就什么也看不到。现三条收口：
  - **落地唯一化**：`useStateSync.ts` 是全前端唯一分发点，每个事件先 `landEvent`（改 store / 快照）再 `eventNote`（进抽屉日志）；`landEvent` 末尾兜底调 `eventNote`，新增事件名不接线就会「有落地无日志」或反之，§5.6.2 的逐事件对照表即是验收单。
  - **`eventNote` 覆盖 11 个事件行**：缓存 6（`hit`/`miss`/`promote`/`corrupted`/`cleanup`/`tempdir-cleared`）+ docker 3（`cleanup`/`orphan-found`/`state-drift`）+ `update:available`/`update:done`。显式豁免只有 `update:progress`（连续量，由升级弹窗进度条承载，逐行落日志会淹没任务流水）；`state:changed`/`service:changed`/`task:*` 的日志已由 `task:*` 逐行给出，不重复。
  - **回流补全**：`docker:state-drift` 在记一行日志之外**额外 `void syncState()`**，让校准结果立刻反映到界面（此前只上报漂移、界面停在旧值）；`.off-events` 面板删除（需求 3），`OfflineView` 的事件流改由抽屉承载。写操作一律 `await` 后端后由快照回流：三根路径变更（`root-set`）走 `App.setCustomRoot` → `Rebind` → `GetState` → `state:changed`，界面路径条的回显只读快照 `env`、**不回填本地输入框**（`EditablePathBar` 的 `val` 只 `watch` prop）；不进快照的数据（缓存条目、备份列表）写后由对应 store 主动重拉（`useCache.load()` / `modals.refreshBackups()`）。
  - i18n：`task.*` 事件文案键使 zh↔en 各 **570 → 601**（集合相等，`check-i18n-keys` 门禁）。

**T111 · 托盘占位** ｜ 依赖：T104
- 交付：`internal/ui/tray.go` `window.go`、`AppTrayMenu.vue`
- 要点：托盘菜单（主题点选/开日志/立即备份/退出）；Linux libappindicator 缺失时降级（§8）
- 接线收口（2026-09-21）：`InstallTray(app, emitter, window)` 把主窗口 `AttachWindow` 给托盘（此前从未绑定，点图标恒为无操作），并注册 `WindowClosing` 钩子接管关闭按钮——驻留则 `Hide()`，否则 `app.Quit()`（`resideOnClose()` 纯函数裁决，`tray_test.go` 四例覆盖）；`App.SetTrayPrefs` 承接设置页两张开关（`prefsStore` → `api/state.ts#setTrayPrefs`，宿主就绪后起闸、`localStorage` 为权威）。真实托盘启动在 `app.Run()`（`SystemTray.New` 延后执行），故删掉了原先那句空操作的 `tray.Run()`
- 验收：三平台托盘图标+菜单可用；「最小化到托盘」开关生效
- **2026-09-22 修正（「退出 phpo」在真实宿主里点了没反应）**：窗口内托盘菜单的 `quitApp()` 只留了一句注释——「生产版由 `internal/ui/menu.go` 调 `app.Quit()`」——于是**两个托盘入口根本不是一个出口**：原生菜单直接 `app.Quit()`（绕过忙锁），窗口内仿真菜单**什么都不做**；Linux 无 libappindicator 时原生菜单不存在，退出入口彻底失效。现收敛为单一出口：门面新增 `App.Quit()`（`a.wails.Quit()`，不置 `restartPending` 故不拉起自身），原生菜单只广播 `ui:command(quit)`，前端 `AppTrayMenu.quitApp` → `api/state.ts#quitApp` → `App.Quit`，忙锁（`task.queueRunning` → `TASK_BUSY` toast，原先的 `window.alert` 在 Wails webview 里不保证可见）两条入口共用。`TestQuitWithoutWails` 锁「宿主未注入即拒绝、且不得置重启标记」；`wails3 generate bindings` 实出前端方法 63→**64**（新增 `Quit`；AGENTS.md §4.1 的「64 个绑定方法」按导出方法计，现 66，口径漂移此前已登记，不动冻结总纲）

**T112 · M1 集成验收** ｜ 依赖：T101–T111
- 验收（里程碑门禁）：原型界面 1:1 运行；doctor 首检弹窗/HomeSetup 向导流程走通（mock）；§10.1 验收清单逐项签认

---

## 阶段 M2 · 底座（纯逻辑，零 Docker 依赖）

**T200 · model 层 DTO** ｜ 依赖：T101
- 交付：`internal/model/` 13 文件（service/site/task/backup/offline/snapshot/update/operation/resource/cache_entry/dto + cleanup/doctor）
- 要点：字段与原型 `state` 结构及 §5.6 事件载荷一一对应
- 验收：`go vet` 通过；JSON 序列化快照测试

**T201 · 路径纯函数** ｜ 依赖：T200
- 交付：`internal/config/paths.go` `config.go`
- 要点：`derivePaths`/`hostToContainer`/`MOUNTS`(5) 从原型逐行直译；`<用户数据目录>`（XDG，Linux `~/.config/phpo`）与 `./`（PHPO_HOME 根，默认值 `~/phpo`）双根目录规则（§4.2 / AGENTS.md §0.1.1）
- 验收：以原型函数输出为金样本的单测全过（含中文路径、Windows 盘符）
- **2026-09-22 修正（缓存根 / 备份根 / 每版本数据目录可自定义，且互斥唯一，AGENTS.md §5.15 / 决策 23）**：此前 `derivePaths` 将 `OFFLINE_ROOT`/`BACKUP_ROOT` 算成 `PHPO_HOME` 的固定子路径、数据目录算成 `{KIND_ROOT}/{version}/data`，界面没有任何入口能改——内网/换机用户只能整台机迁 PHPO_HOME。现 `config.yaml` 补三个选填项（`offline_root` / `backup_root` / `services.{kind}.{ver}.data_dir`），派生规则是**唯一生效值**：非空即自定义、默认根那一侧**不再被读也不被写**，空串即清除并回落默认（不存在「两处并存」）。落点：`ApplyRootOverrides` / `ApplyDataDirs` / `DataDirFor` / `HasCustomDataDir` 为唯一出口，快照 `env` 补 `OFFLINE_ROOT` / `BACKUP_ROOT` / `{KIND}_{VER}_DATA_DIR` 三类键供回显；`ValidateRootPath` 只守路径安全（硬红线 3）。**改根即换图**：`app.go` 的 `setCustomRoot()` 走 `guard(root-set)` → 队列非空即 `errs.TaskBusy`（不得在任务中途抽掉在跑的门面）→ 写 `config.yaml` → `container.Rebind()`（按 `rootsKey` 幂等）→ `GetState()` → `state:changed`；**不建任务**（改的是配置图，不是 Docker 资源）。用例：`TestRootOverrides_CustomWinsExclusively` / `TestRootOverrides_SurviveHomeExpansion` / `TestDataDirOverride_OnlyAffectsDataMount` / `TestRootOverrides_RejectTraversalOnly` / `TestConfigStore_RootAndDataDirPersist` / `TestRebindOnCustomRootChange`。

**T202 · 版本策略校验** ｜ 依赖：T200
- 交付：`internal/config/versions.go`（校验）、`pkg/version/compare.go` `semver.go`、建议版本在 `frontend/src/constants/service.ts` 的 `SVC_META.suggested`（建议值，非白名单；仓库内无 `configs/` 目录）
- 要点：仅路径安全校验：非空/无 `/` `\`/无 `..`/无 `\x00`/**≤128**/trim（§5.4）；禁止字符集白名单
- 验收：`internal/config/versions_test.go` 边界用例：128/129 字符、含点版本 `8.4.3`、`../etc`、含空格、空串

**T203 · 密码策略** ｜ 依赖：T200
- 交付：`internal/config/password.go`（genPassword）、`internal/config/configstore.go`（`services.{kind}.{version}.password`）、`frontend/src/components/business/PasswordField.vue`（壳）
- 要点：明文写 `config.yaml`（`<用户数据目录>/` = XDG 用户配置目录内 `phpo` 子目录，唯一配置权威；不再有 PHPO_HOME 根下的 `./.env` 与 SQLite env 表）；默认 `123456`；可空、长度不校验、UI 可查看（§1.5 六规则）；禁止任何加密/校验路径
- 验收：空密码、1 位、100 位、含空格密码写入读取一致；`config.yaml` 权限 600/NTFS ACL（§8）；快照 `env` 扁平键 `{KIND}_{VER}_PASSWORD` 回显与落盘一致

**T204 · 端口引擎** ｜ 依赖：T200
- 交付：`pkg/port/suggest.go` `probe.go` `sequence.go`、`internal/store/port.go`；占用判据在 preflight 内联复用 `store.CollectUsedPorts`（无独立 `internal/preflight/portprobe.go`），本机 TCP 实探 `port.Probe` 仅供 doctor 的「80 端口可用」检查
- 要点：站点端口默认 80；**新建站点**占用时**不顺延**——`Options.KeepOnConflict` 原样保留用户所填端口并置 `Result.Occupied`，交上层只告警 + 站点降级（§5.8）；**改已有站点**端口才在 1–65535 顺延首个可用（不报错、无窗口上限）；服务端口占用报 `portInUse`；冲突集单一来源 `store.CollectUsedPorts`（数据服务端口 + 既有站点端口，对齐原型 collectUsedPorts）
- 验收：占用矩阵单测——建站 80 占 → 端口仍为 80 且 `Occupied=true`（`TestValidateKeepOnConflict`）；改站点端口 80 占→81、80–179 占满→180 继续向上无耗尽错误（`TestFindNextAvailableUpThenWrap` / `TestValidateOccupiedAlwaysAdvances`）

**T205 · SQLite 存储层** ｜ 依赖：T200
- 交付：`internal/store/store.go` `sqlite.go` `migrate.go` `migrate/0001–0008.sql` `snapshot.go` `sync.go` `port.go` `operation.go` `trash.go` `offline.go` `backup.go`
- 要点：`modernc.org/sqlite`（CGO_ENABLED=0）；0002 离线表、0004 审计表、0005 缓存清单表、0006 站点伪静态列、0007 下线 `dir_ready`（改由快照按「两根已持久化 + 目录存在」派生）、0008 任务账本列（`task_id/label/logs`）；SQLite 仅存运行态，密码/端口/两根归 `config.yaml`；**延迟建库**——两根未写入 `config.yaml` 前不打开 DB（首启零落盘）
- 验收：迁移 up/down 往返测试；崩溃恢复（WAL）测试

**T206 · preflight 框架** ｜ 依赖：T201–T205
- 交付：`internal/preflight/preflight.go`（`Run` 分派 + `needsHome` 集合，无独立 `common.go`）`validators.go`、`pkg/errs/errors.go` `codes.go`
- 要点：19 action switch-case 路由 + NEEDS_HOME(17) 集合 + PF 错误码表 + 返回 `{ok,errors,warnings,adjusted}`；警告不阻止（§0.2 规则 16）；UI 即时反馈仅为预览，最终裁决在此（规则 14；落地方式见 `docs/接口契约.md` §1.1——`app.go` 的 `guard()` 在提交任务前调 `Run`，只消费 `errors`）
- 验收：19 action 空壳用例全注册；`TestActionAndNeedsHomeCounts` 断言 `len(AllActions) == ActionCount == 19`、`needsHome` 命中数 `== NeedsHomeCount == 17`（总纲 §0.3 权威值）；**裁决已接入生产**：`app.go` 的 `guard()`→`runGuard()` 在 20 个写入口调 `preflight.Run`（19 action 全覆盖，`Reinstall`/`SiteAddHosts` 有意例外），非 `Ok` 即拒绝且不创建任务，`Warnings` 不阻断；`TestGateEveryActionWiredInFacade` 逐 action 对账 `app.go` 源码，漏接即失败（见 `docs/接口契约.md` §1.1）
- **2026-09-22 修正（新增 `root-set` / `cache-import` 两个 action，AGENTS.md §5.15 / 决策 23）**：原型的 17 action 里没有「改根」与「手工导入」这一类入口——缓存根/备份根/数据目录此前只有默认值，`install` 之类也就无从校验它们。现补 2 个（`ActionCount 17→19`、`NeedsHomeCount 15→17`，两者都被 `TestActionAndNeedsHomeCounts` 锁死）：`root-set`（`rules_root.go`）只守路径安全（硬红线 3）+ 空串即清除，数据目录在目标版本运行时出 `warn`「要重建容器后才生效」而不阻断；`cache-import`（`rules_cache.go`）只裁决坐标合法：`kind`/`version` 与安装同判据、`field` 限定 `image`/`apk`/`pecl` 三种落点（apk/pecl 只允许 php）；**preflight 不碰文件系统**，源文件是否存在由导入步骤报 `errs.FileMissing`（新增错误码，`errs.CodeCount 27→28`）。两个新 action 都进 `needsHome`（写 `config.yaml` / 写缓存根，两根未就绪即拒绝，否则违反「首启零落盘」）。`guard()` 写入口随之 18→**20**。

**T207 · preflight 服务类规则（6）** ｜ 依赖：T206
- 交付：`internal/preflight/rules_service.go`
- 范围：install / uninstall / service-start / service-stop / update-config / service-config
- 验收：正反用例各≥2（未装版本启动、卸载在用 PHP 被拦等）

**T208 · preflight 站点类规则（6）** ｜ 依赖：T206
- 交付：`internal/preflight/rules_site.go`
- 范围：site-add / site-remove / site-port / site-vhost / php-switch / rewrite；php-switch 必须校验容器在场且上游为 `php-{version}-fpm:9000`（硬红线 1）；根在 WWW_ROOT 外 → `PF.rootOutsideWww` 警告；**建站端口冲突走 `conflictKeepWarn`：保留原端口、只告警、不写 `adjusted.port`**（§5.8），**改站点端口走 `conflictAdvance`：顺延并写 `adjusted.port`**；nginx 未装是建站唯一服务门禁（阻断），nginx 未运行 / PHP 未装 / 端口占用只降级不阻断
- 验收：对齐原型 site-add/site-port 分支行为逐条回测（含 warnings 不 ok:false）

**T209 · preflight 运维/缓存类规则（5+2）** ｜ 依赖：T206
- 交付：`internal/preflight/rules_ops.go` `rules_cache.go` `rules_root.go`
- 范围：extensions / backup / restore / backup-delete / offline-prune + 生产新增 root-set / cache-import
- 验收：19 计数收口（6+6+5+2）；offline-prune 禁止清「正在使用」缓存（§5.14.12）

**T210 · 任务引擎骨架（三段式）** ｜ 依赖：T206
- 交付：`internal/task/manager.go` `task.go` `step.go` `funcstep.go` `emitter.go` `rollback.go` `ledger.go`
- 要点：preflight → task → applyStateChange 唯一写路径（硬红线 5）；Step 接口五法 Name/Execute/Rollback/Cleanup/Cancelable（§5.13.3）；M2 阶段 Executor 为 Noop；Manager 为**串行 FIFO 队列**（并发写排队不报错，同标签重复提交 `ErrQueued`，队列内容随 `state:changed` 的 `Snapshot.Tasks` 广播）；任务收口写任务账本（`operations.task_id/label/logs`，日志尾部保留 500 行），账本失败不改变任务结果
- 验收：静态扫描脚本证明无旁路写（UI 层不得直改 store 状态字段）；task:log/progress/done 事件按协议发射；`duration` 为 `time.Duration` 纳秒；失败原因取末条 `err` 级日志 + 账本 `operations.error/logs`
- **2026-09-22 修正（队列详情要能定位到被操作对象）**：`model.TaskBrief` 补 `kind` / `version` 两列，由 `briefOf` 从 `model.TaskMeta` 逐字透传（运行项与排队项共用同一出口），7 类服务任务（install / reinstall / start / stop / remove / extensions / config-save）在 service 层统一带 `serviceMeta(op, kind, version)`（`AppService.one` 改收 meta）。前端 `ServiceView` 据此在**被操作的那一版卡片**挂 `.status-pill.pill-warn`「运行中… {step}/{total}」/「排队中」，复用既有 `task.running` / `task.queued`（无新文案键）。**不得靠 `label` 文案反推**，也**不因任务在跑就禁用按钮**——FIFO 排队合法，拦下来违反最小限制原则。用例：`TestBoardCarriesServiceTarget`、`TestAppService_ServiceTasksAdvertiseTarget`。**同轮把 `domain` 也带进面板**（`TaskBrief` 第 4 列，服务类任务留空、站点类任务由 `SiteService` 的 4 处任务字面量携带）：站点列表的 `busyPill(domain)` 据此在「健康」旁亮同一枚 pill，建站不点亮（新站点尚未进快照、无行可标）。用例：`TestBoardCarriesSiteTarget`
- **2026-09-22 修正（抽屉两栏 70/30 + 队列新任务置顶 + 显示态三元组，AGENTS.md §5.6.1）**：上面的 busy pill 与抽屉队列统一为**六个 UI 显示态**（`taskStore.ts#displayOf`：`waiting` 等待中 / `running` 执行中 / `done` 已完成 / `failed` / `cancelled` / `unknown` 预留兜底），文案改口为 `task.running`＝「执行中 / Running」、`task.queued`＝「等待中 / Waiting」，新增 `task.done`、`task.dsUnknown` 两键（zh↔en 各 **570**）——**后端 `model.TaskStatus` 仍 4 个、事件仍 17 个**，显示态只是快照分区 + `task:done` 的派生量。队列区由 chip 流改为 `.drawer-main`（70%，日志）+ `aside.drawer-queue`（30%，竖排行）；行位**只随提交先后**（记录池 newest-first，运行中任务不因取得执行权而下移）；`syncBoard` 为 `Running` **和每一个 `Pending` 项**建行，故一提交即上屏、不等执行权；被撤回项若正被选中，选中在同一次快照里回落到运行中任务，不留「状态未知」的幽灵详情。零后端改动（`SetQueueWatcher` 本已在入队/移交/撤回三处重发快照）。
- **2026-09-22 修正（任务终态必须回流，失败也不例外）**：① `task.Run` 的成功路径改为延迟落库闭包、`defer` 判 `aborted`——取消/超时场景下容器已建出来也不记「已装」（此前先在步骤里落库，取消后库里留着一条运行态根本没有的服务）；② `Manager` 的 panic recover 补 `release()`，消除 `tasksRunning` 永久为 `true` 把后续**所有**写操作锁死（panic 一次即整应用只读）；③ `manager.run` 在 `release` 之后、发终态事件之前**无条件**校准，失败任务也让「实际已变成什么状态」回流（§5.13.9「每任务后校准」此前只在成功路径兑现），失败时 `BuildSnapshot` 的报错另记一条 `err` 级 `task:log` 并跳过本次校准，不篡改任务结果

**T211 · 离线缓存核心** ｜ 依赖：T201、T205
- 交付：`internal/config/offline.go`（缓存/临时目录路径常量，仓库内无 `configs/` 目录）、`internal/cache/` 10 文件（manager/lookup/manifest/image_cache/extension_cache/tempdir/promote/verifier/cleaner/stats）、`internal/model/cache_entry.go` 落表
- 要点：目录结构严格按 §5.14.2（含 manifest.json schema）；缓存命中优先级「离线缓存 > 本机 Docker 镜像库 > 网络」（第二级见 §5.14.3 v2.9.6：镜像未命中先 `ImageExists` 探本机，已有即零网络 `docker save` 重建缓存）；SHA256 校验在 `internal/cache/verifier.go`（直接用标准库 `crypto/sha256`，无独立 `pkg/hash/`）；ClearTempDir 四必清（成功/失败/取消/启动）+ 每次发 `cache:tempdir-cleared`；缓存与 Docker 资源解耦（§5.14.13）
- 验收：`internal/cache/cache_test.go` 十例全绿（命中/未命中提升/编译失败清临时/损坏回退/启动扫残留/无后端/保守清理/统计等）+ 端到端 `test/integration/m6_offline_live_test.go`；损坏缓存注入 → `cache:corrupted` + 落入未命中分支（先探本机镜像库再决定是否联网）
- **2026-09-22 修正（缓存根可自定义 + 手工导入单条缓存，AGENTS.md §5.14.9a / 决策 23）**：① **全链路跟着生效根走**——`internal/cache/` 的查缓存/落缓存/清单/校验/清理/统计本就统一取 `m.env.Offline*` 系列助手，但 `OFFLINE_ROOT` 在 HEAD 只会由 `derivePaths` 算成 `{PHPO_HOME}/offline`，用户即使手工放过一份缓存也在别处。现该字段可被 `ApplyRootOverrides`（T201）整体换掉，缓存侧一行不改即全部跟着走，**临时目录 `./{kind}/{version}/ext/` 不随缓存根移动**（它仍从 PHPO_HOME 派生，§5.14.4 四必清口径一字未改）。② **新增「手工导入」**（`OfflineImportFile` → `OfflineService.ImportEntry`，任务 type `cache-import`，两步「读取源文件」/「写入缓存」）：把任意 `image.tar` / `.apk` / `.tgz` **复制**进缓存根（`placeFile(keepSrc=true)`，原件留在用户选的位置不搬走）、算 SHA256、写 `manifest.json`、发 `cache:promote`；**不解镜像、不 `docker load`**（缓存与 Docker 资源解耦，§5.14.13），下次安装该版本才命中零网络。源文件不存在报 `errs.FileMissing`，失败不留半条缓存。这是画像 F 在「缓存从未建过」情形下的唯一入口（另两条是改根指向既有目录、整目录搬运）。用例：`TestImportImageCopiesSourceAndRegistersManifest` / `TestImportExtensionLandsInCustomRoot`（后者同时锁「导入落在自定义根而非 `PHPO_HOME/offline`」）。

**T212 · 升级检查骨架** ｜ 依赖：T200
- 交付：`internal/updater/checker.go` `scheduler.go`（启动 + 24h 触发，暂只发 `update:available`）
- 验收：`internal/updater/updater_test.go` 以 `httptest` 假发布清单覆盖有新版/无新版/网络失败三用例；失败仅提示「不影响使用」（§5.7）

**T213 · M2 集成验收** ｜ 依赖：T200–T212
- 验收：无 Docker 环境 CI 全量单测通过；`scripts/check-i18n-keys.go` `check-templates.go` `check-docker-naming.go` `check-cache-manifest.go` 四脚本上线并纳入 CI；§10.2 验收清单逐项签认

---

## 阶段 M3 · 服务线（PHP + Nginx，首次接真实 Docker）

**T301 · Docker 客户端与前置检查** ｜ 依赖：T213
- 交付：`internal/engine/client.go` `health.go`
- 要点：Docker 未装/未运行则不能启动服务（硬红线 7），报人话 + 下载链接（§5.7）
- 验收：mock 三种 Docker 状态；版本 <20.10 仅警告

**T302 · 镜像 load/pull/save** ｜ 依赖：T301
- 交付：`internal/engine/image.go` `registry.go`、`pkg/dockerutil/imageref.go` `wait.go`
- 要点：SDK `ImageLoad`/`ImageSave`；进度流式上报
- 验收：本地 tar 载入→删除→再载入幂等

**T303 · 缓存优先安装管道** ｜ 依赖：T211、T302
- 交付：`internal/task/steps/steps_service.go`（install 段）
- 要点：严格按 §5.14.3 流程图：lookup→命中 load→发 `cache:hit`；未命中先 `ImageExists` 探本机镜像库——已有即 `docker save`（`cache:miss` 的 `action=local`，零网络）、没有才 `pull`（`action=pull`）→save→verify→promote→更新 manifest→清临时→发 `cache:miss`+`cache:promote`+`cache:tempdir-cleared`；任务开始先清残留临时目录
- 验收：断网 E2E——预置 `./offline/php/8.4/image.tar`（`./` = PHPO_HOME 根，装机向导选定）后安装零网络成功；无缓存但本机 Docker 已有镜像 → 零网络重建缓存并安装成功；两者都没有且断网 → 明确失败且无脏状态
- **2026-09-22 修正（安装期端口/密码一次带过）**：`App.Install` 携带 `model.InstallOptions{Port,Password}`——后端**先跑 `install` 裁决**（端口占用即 `portInUse` 且不落任何配置），通过后才写 `config.yaml` 再建容器。**不得改成「先 `SetServicePort` 再 Install」**：`update-config` 要求版本已安装，未装态会被 `notInstalled` 拦下，等于任何带端口的服务都装不起来（对应原型 `applyStateChange` 的 `type==='install'` 分支）。同轮把安装期扩展从「只进 demo 日志」改为真接 `ExtApply`

**T304 · 容器/网络/卷装配** ｜ 依赖：T303
- 交付：`internal/engine/container.go` `network.go` `volume.go` `mount.go` `inspect.go`、`pkg/dockerutil/naming.go`（容器 inspect 在 engine 侧，`pkg/dockerutil/` 只有 naming/imageref/wait）
- 要点：命名 `phpo-{kind}-{version}` / `phpo-network` / `phpo-{kind}-{version}-{purpose}`；网络别名 `{kind}-{version}`（如 `php-8.4-fpm`）；挂载走 MOUNTS 表
- 验收：`check-docker-naming.go` 扫描无违规资源
- **2026-09-22 修正（nginx 宿主端口发布闭环）**：① 发布集 = 用户配的 nginx 基准端口 ∪ 站点端口，**单一权威 `SiteService.PublishPorts`**（装配层双向注入 `SetNginxPublisher` / `SetNginxPortSource`），不再有两处各算一份的漂移源；② 重发布只对「本次新增」的宿主端口做 TCP 实探（自身已发布端口经 `engine.PublishedPorts` 排除——Docker 的宿主绑定本来就占着它，自探必假阳），且实探**排在 Pre-Clean 之前**，判占用不得先删掉正在服务的容器；③ `port.InUse` 只认 `EADDRINUSE`，非 root 绑 <1024 的 `EACCES` 不算占用（否则 mac/linux 上每个特权端口都被报成被占）。前端同步：nginx 端口行恢复可行内改（与原型一致），删掉后端从不发的虚构 `NGINX_PORT` 键与「此处不可修改」文案，「重建生效」按钮覆盖到 nginx
- **2026-09-22 修正（重发布不得启停用户在控的服务）**：`RepublishNginx` 补两道闸——① nginx 未运行（含容器被手工删掉）即整体跳过：站点写链路不得把用户主动停掉的服务悄悄拉起并占回宿主端口，停机期间站点保持降级、端口等 nginx 下次启动的就绪补齐再绑（判据取 Docker 实际态 `ContainerRunning`，不看库里的 running 标记）；② 目标发布集与容器当前绑定（`PublishedPorts` 实读）一致即不重建：重建等于把整站白闪断一次，而端口校对会被就绪补齐反复触发。同轮把 `ReconcileServe` 拆成 `checkPorts` 两档：就绪入口每次都校对宿主端口（停机期间删过站会留下陈旧绑定），站点写链路的 `healFreed` 走 `false`（本次已在步骤里发布过，只有真补齐了别的站点 vhost 才再发一次），避免同一次操作重建两遍。用例：`TestRepublishNginx_SkipsWhenStopped` / `TestRepublishNginx_SkipsRebuildWhenPortsUnchanged`，`TestSiteService_ReconcileServe_HealsAfterNginxReady` 的幂等断言从「不重复发布」改为「仍以权威发布集校对一次」。
- **2026-09-22 修正（删除容器不得连带删数据卷）**：`engine.Client.RemoveContainer` 曾带 `RemoveVolumes: true`，而 Pre-Clean 删同名容器是**每个**写操作的第一步——「卸载默认保留 volume」（§0.2 规则 20、§5.13.7）在真实链路上从未兑现，删掉的卷只能靠 `docker volume rm` 的副作用找回。现只留 `Force: true`（停删合一、不存在视为成功的幂等语义不变）；本项目持久化全在宿主 bind 目录（MOUNTS 表），容器不留卷，`RemoveVolumes` 唯一效果是误伤镜像自带的匿名卷。用例：`internal/engine/container_wire_test.go` 的 `TestRemoveContainer_KeepsVolumes`（逐字段锁 `RemoveOptions`）/ `TestRemoveContainer_MissingIsNoop`，以及 T306 的 `TestReinstall_AppliesNewHostPortAndPreservesVolumes`
- **2026-09-22 修正（数据服务可按版本自定义数据目录，AGENTS.md §5.15 / 需求 7）**：MOUNTS 的 `data` 行此前只能算出 `{KIND_ROOT}/{version}/data`，数据必须留在 PHPO_HOME 之内——数据库文件想吃系统盘/独立盘的用户无处安放。现 `config.Env.ResolveMounts` 中 **仅 `sub=="data"` 一行**改走 `DataDirFor(kind,version)`（自定义即用、否则回落原路径），`conf` / `logs` / `initdb` 与容器路径 `To` 一律不动，即「换位置」而非「多一层」。配套两处：`workdir.prepareService` 在 `HasCustomDataDir` 为真时**跳过**默认 `data` 目录的创建（否则等于凭空造出一个第二数据目录，违反 §0.2 规则 24）；`ExpandEnvHomes` 重派生时必须把自定义根与 `DataDirs` 一并 `ExpandHome` 后带回——漏掉即把 `~/data` 悄悄换回默认根。运行中的版本改数据目录只出 `warn`（挂载只在建容器时落定，须走「重建生效」，与改端口同口径），不阻断。用例：`TestDataDirOverride_OnlyAffectsDataMount`（锁「只有 data 一行变，其余行不变」）。

**T305 · 幂等三阶段（Pre-Clean/Execute/Post-Verify）** ｜ 依赖：T304
- 交付：`internal/engine/idempotent.go` `verify.go` `cleaner.go`
- 要点：操作前清同名/冲突资源、操作后验证「Docker 实际状态 ≡ SQLite 状态」；失败全回滚（§5.13.3）
- 验收：install/uninstall/start/stop 各重复执行 100 次结果一致、零残留

**T306 · 状态校准 + 生命周期服务** ｜ 依赖：T305
- 交付：`internal/engine/calibrate.go`、`internal/service/lifecycle_service.go` `registry.go` `php_service.go` `nginx_service.go`
- 要点：启动时 + 每任务后 + 手动校准；stopped 状态反向推导（原型映射）；重装 = 卸载保留卷 + 安装复用卷（§5.13.12）
- 验收：手动 `docker kill` 后校准一致并产生漂移事件（`docker:state-drift`）
- **2026-09-22 修正（启停必须等稳定 running、失败自带取证，AGENTS.md §5.18.3）**：`StartContainer` 原先一次 `ContainerStart` 后即返回，运行态校验随即比对——而容器 `State.Running` ≠ 服务进程就绪（entrypoint 有前置脚本），崩循环的服务被判成「启停成功但校验失败」。现按当前 `ContainerStatus` 分流（已 running 不动 / `restarting` 与其他一律 `ContainerRestart` / 其余 `ContainerStart`），`waitRunning` 轮询到 running 后再静默 `startHold = 2s` **复验**（躲开「起来即崩」窗口），超时 `startWait = 12s`、间隔 `startInterval = 300ms`（三个量是 `var`，供单测收紧到毫秒级）；失败消息 `startFailureMsg` 带**容器名 + 状态 + 退出码 + 容器日志尾部 5 行**（`LogTail` 经 `ExecStream` 去帧）；`StopContainer` 只对 `needsStop`（running/restarting）执行。用例：`internal/engine/container_wire_test.go`。
- **2026-09-22 修正（外部删容器后不得虚报「运行中」，且必须留自救入口）**：① `Calibrate` 原先把「期望已装但容器不存在」一律当纯存在性漂移只上报 `Drift.Missing`、不改运行态，于是 `docker rm` / Docker Desktop 重置之后 SQLite 仍记 `running=1`——界面一直显示「运行中」，点「启动」又因 `no such container` 永久失败。现对「期望运行但容器缺失」产出 `running=false` 的 Correction（**installed 记录原样保留**，数据与重建入口都还在，不自动删建）。② `LifecycleService.Start` 补 `ContainerExists` 分支：容器不在即按当前落库配置走一次 `Reinstall`——php 这类不发布宿主端口的服务卡片上没有重建按钮，不补这条就是彻底死路。③ `specFor` 装配 php 时按本机实际态现取镜像：`ImageExists(phpo/php:{ver})` 为真即用扩展链路 commit 出的固化镜像，否则才用基座 `php:{ver}-fpm`；一律用基座等于装/重建一次就把已启用扩展静默抹掉而 `php_extensions` 仍记着（§5.13.1 一致性），**探针报错必须上抛**（静默当「本机没有」= 把抹除伪装成正常重建，§5.14.12）。用例：`TestStart_RecreatesMissingContainer` / `TestStart_WithoutInstalledDoesNotCreate` / `TestReinstall_FailedCreate_CalibrateClearsGhostRunning` / `TestReinstall_PhpUsesCommittedExtImage` / `TestReinstall_ImageProbeFailureIsNotTreatedAsMissing`

**T307 · 模板引擎** ｜ 依赖：T213
- 交付：`internal/template/embed.go` `render.go` `templates/`（php×2、nginx、mysql、pgsql×2、redis、vhost 共 7 文件，5 服务）
- 要点：`DEFAULT_CONFIGS` 5 服务 7 文件逐一对齐原型内容；按服务种类分不按版本分（§5.3）
- 验收：`check-templates.go` 渲染快照与原配置 diff 为空

**T308 · 取消与回滚真化** ｜ 依赖：T305
- 交付：`internal/task/steps/steps_service.go`（cancel/rollback 路径）
- 要点：任务取消 → 清临时目录（必清时机 3）→ 回滚到操作前；`cancelTask` API + 前端取消按钮接真
- 验收：pull 中途取消：无半截镜像、无临时目录残留、状态回原点

**T309 · M3 集成验收** ｜ 依赖：T301–T308
- 验收：PHP 7.4/8.1/8.3/8.4 与 nginx 真实装/卸/启/停；任意字符串版本号可装（路径安全内）；缓存四事件前端可见；§10.3 清单签认

---

## 阶段 M4 · 站点闭环

**T401 · vhost 管理器** ｜ 依赖：T309
- 交付：`internal/vhost/manager.go` `parse.go` `upstream.go` `rewrite.go` `sync.go` `validate.go`、`internal/template/templates/vhost.conf.tmpl`
- 要点：VHosts 9 方法后端化；`replaceListen`/`replacePhpUpstream`（正则 `set \$php_upstream [^;]+;`，上游 `php-{version}-fpm:9000` 保留点号，硬红线 1）；伪静态 `REWRITE_PRESETS`(9)；vhost 指令放开（§5.10）但写入前 `nginx -t` 必过（硬红线 2）
- 2026-09-22 修正：`nginx -t` / `nginx -s reload` 的目标容器名**不得装配期写死**（曾是 `phpo-nginx-alpine`，而 nginx 版本开放输入 §1.6）——现由 `vhost.ContainerFunc` 在每次调用时从权威快照 `Installed[nginx][0]` 现取，未安装即报错、不回落到默认名，否则命令 exec 到不存在的容器上等于硬红线 2 恒失效
- 验收：`internal/vhost/{parse,rewrite,upstream,manager,sync}_test.go` 覆盖解析/改写（含注释嵌套、多 location、IDN 域名，如 `TestParse_IDNDomain`）；坏配置注入 → `nginx -t` 拦截不落盘；`internal/vhost/nginx_exec_test.go` + `internal/app/nginx_container_test.go` 锁死容器名随快照变化、解析失败不起进程

**T402 · hosts 管理（三平台提权）** ｜ 依赖：T401
- 交付：`internal/vhost/hosts/`、`internal/util/fs.go`
- 要点：Windows UAC / macOS osascript admin / Linux polkit（§8）；不可写时人话提示（§5.7）
- 验收：三平台加/删 `demo.test → 127.0.0.1` 实测；拒绝提权时站点仍创建成功仅警告

**T403 · site add / remove** ｜ 依赖：T401、T402
- 交付：`internal/service/site_service.go`、`internal/task/steps/steps_site.go`、`frontend/.../SiteAddModal.vue` 接真
- 要点：域名零限制（§5.11）；根路径允许 WWW_ROOT 外（警告）；幂等建站；删除走回收站（`engine/trash.go`，7 天）；**删站顺带回收该域名的 hosts 条目**（`steps.RemoveHosts`，提权被拒仅警告不阻断、回滚时加回，与建站同口径）；**建站失败连 hosts 一起回滚**（`steps.AddHosts.Rollback` 撤下本步写入的行——末步「发布端口到 Nginx」失败时不留指向未建成站点的孤儿解析；仅「真写入过」才撤，幂等跳过与提权告警分支不动文件）
- 验收：重复建站幂等；删站后 hosts/vhost/目录状态一致（hosts 不得残留指向已删站点的行）；回收站恢复用例；`TestSiteService_Add_RollsBackHostsWhenLaterStepFails` + `steps_site_test.go`（`TestAddHostsRollback*` / `TestRemoveHostsRollback*` 各 3 档）锁死两侧回滚对称

**T404 · 端口冲突 UI 闭环（建站告警降级 / 改端口顺延）** ｜ 依赖：T403
- 交付：`frontend/.../SiteAddModal.vue`、`composables/usePortSuggest.ts`（行内改端口入口在 `views/SitesView.vue` 端口列）
- 要点：**新建站点**端口占用**不顺延、不改用户所填端口**（§5.8）——`preflight('site-add')` 出 `conflictKeepWarn` 警告、不带 `adjusted.port`，经 DangerConfirm 告警后以降级态建站（目录/hosts/落库照常，vhost 暂不落盘、端口暂不发布）；**改已有站点端口**才消费 `preflight('site-port')` 的 `adjusted.port`（1–65535 首个可用），DangerConfirm 展示「80→81 已调整」，确认后 `setSitePort` 落库
- 验收：80 占用下建站 → 确认文案为降级告警、端口仍为 80、列表当场可见；行内改 80 → 展示顺延后的新端口，确认后落库发射 `state:changed`；腾出端口后任一站点写操作或 nginx 就绪（`ReconcileServe`）自愈发布
- **2026-09-22 修正（自愈触发面 + 「nginx 未运行不得编辑 vhost」口径）**：① 自愈此前只挂在少数事件上，缺口按纯函数 `siteHealedBy(kind, op)` 收口——`AppService.healStep` 挂在 **nginx 装/重建/启动**（写 vhost 要的 `nginx -t` 只能在运行中的容器里跑）、**php 装/重建**（缺上游而降级的站点有了 fastcgi 目标）、**mysql/pgsql/redis 卸载**（让出被占端口）之后；php 卸载与 nginx 停止只会让站点更降级，补齐必然空转故不挂。补齐失败只记任务日志，不判整个安装任务失败（容器已按请求装好，站点下次就绪事件仍可自愈）。② 站点写操作**让出**的端口同样要补齐别的站点：`Remove`（删站）与 `writeVHost`（改端口等）在 **Apply 段末尾**走 `healFreed`——步骤阶段库里仍是旧占用表，那时补齐会照旧判「端口被占」而空跑；补齐失败只记一行 `task:log` 不上抛（本次操作已成功，回滚整任务与本意相反）。③ **编辑站点与建站门禁不同**：`site-port` / `site-vhost` / `php-switch` / `rewrite` 四个动作过「站点存在」后再过 `nginxServing()`——未装报 `nginxNeeded`、已装未运行报 `errs.NotRunning`，**均阻断而非降级**（前端 `usePreflight` 同文案逐字镜像）。理由：建站没有旧 conf，未运行只降级即可；编辑必须写盘，而写盘前的 `nginx -t`（硬红线 2）只能在运行中的容器里执行；且 `ReconcileServe` 只补**缺失**的 vhost（`os.Stat` 命中即跳过），若放行编辑再跳过写盘，盘上会长期留着与库内不一致的旧 conf 且再无自愈入口。

**T405 · PHP 切换 10 步流程** ｜ 依赖：T404
- 交付：`site_service.go#SwitchPHP`、`frontend/.../PhpSelect.vue` `usePhpSwitch.ts`
- 要点：严格按 §5.5 十步（Preflight→读取→计算→校验→写盘→nginx -t→reload→落地→返回→验证）；禁止模糊匹配（硬红线 1）
- 验收：8.3↔8.4 往返切换后 vhost 上游逐字符等于期望值；`nginx -t` 失败步骤回滚原 vhost

**T406 · 伪静态 + vhost 编辑 UI 闭环** ｜ 依赖：T405
- 交付：`RewriteModal.vue`、`VhostConfigModal.vue`、`api/config.ts`
- 要点：9 预设选择即时预览；手改 vhost 保存前 preflight + `nginx -t`
- 验收：每预设生成内容与原型 `REWRITE_PRESETS` diff 为空；手改坏配置被拦并展示 nginx 错误原文

**T407 · M4 集成验收** ｜ 依赖：T401–T406
- 验收：`demo.test` 建站→浏览器访问→切 PHP→伪静态→改端口 全链路真跑通；§10.4 清单签认

---

## 阶段 M5 · 数据服务

**T501 · MySQL 全生命周期** ｜ 依赖：T407
- 交付：`internal/service/db_service.go`（mysql 段）、`internal/config/configstore.go`（`services.{kind}.{version}.{password,port}` 落 `config.yaml`）、`internal/template/templates/mysql/my.cnf.tmpl`、`MysqlView.vue` 接真
- 要点：卷 `phpo-mysql-{ver}-data` 持久；initdb 目录挂载；镜像安装复用 T303 管道
- 验收：装启停卸 + 建库写数 + 卸载重装数据不丢

**T502 · PostgreSQL 全生命周期** ｜ 依赖：T501
- 交付：`db_service.go`（pgsql 段）、`internal/template/templates/pgsql/*.tmpl`×2、`PgsqlView.vue`
- 要点：`pg_hba.conf` 预设放行容器网段；同上卷保留策略
- 验收：同 T501；跨版本（16↔17）并存不串数据
- **2026-09-22 修正（反复启停后启不来：日志出口必须离开宿主 bind 目录，AGENTS.md §5.18 / 决策 27）**：真机现象 `运行态校验失败：phpo-pgsql-17 期望 running=true，实际 false`，容器一直 `Restarting (1)`。根因是 `postgresql.conf` 的 `logging_collector = on` + `log_directory` 指向宿主 bind 挂进来的 `./pgsql/{ver}/logs`——那目录由宿主用户建（0755），容器内 postgres 是另一个 uid，建日志文件即 `Permission denied` → FATAL 退出 → `unless-stopped` 无限重启。两处收口：① 模板改 `log_destination = 'stderr'` + `logging_collector = off`，日志由 Docker 收集（`docker logs phpo-pgsql-{ver}`）——这是**唯一一处**对冻结原型 `DEFAULT_CONFIGS` 的生产偏离，已在 `scripts/check-templates.go` 的 golden 注明，原型 SSOT 与 demo-only 的 `frontend/src/constants/configs.ts` 不改；② `prepareService` 对已存在配置一律保留不覆盖，旧装机的坏配置永远等不到被换，故「**启用**」路径也跑一次 `healPgLogging`（`internal/service/workdir.go`）：按原文精确匹配旧三行即**原地**替换成新两行（`postgresql.conf` 是单文件 bind，写临时文件再 rename 会换 inode、容器读到的还是旧那份），含 `logging_collector = on` 但已非默认写法则**不改写**只提示核对。**真机取证（同一轮稍后）**：现场那份坏 conf 已按同一段新两行手工落地并 `docker restart phpo-pgsql-17`，服务恢复 `database system is ready to accept connections`、宿主 logs 目录保持为空、`RestartCount` 归零；同一轮把这条因果锁成 live 用例 `test/integration/g4_pgsql_heal_live_test.go`（`PHPO_LIVE=1`；刻意装 `pgsql 17-alpine` 而非 `17`，避开真机同名容器的 Pre-Clean）——旧配置下裸 `StartContainer` 必超时失败且报错自带容器日志尾部（`could not open log file`），同一份坏配置经 `AppService.Start` 即自愈并等到就绪。用例：`internal/service/workdir_test.go`、`test/integration/g4_pgsql_heal_live_test.go`。

**T503 · Redis 全生命周期** ｜ 依赖：T501
- 交付：`internal/service/redis_service.go`、`internal/template/templates/redis/redis.conf.tmpl`、`RedisView.vue`
- 验收：持久化开关、PING 探活、卷数据保留

**T504 · 密码 UI 完整落地** ｜ 依赖：T503
- 交付：`PasswordField.vue`（👁/📋/↺）、`api/env.ts`、`internal/service/env_service.go`、`internal/service/lifecycle_service.go#Reinstall` + `app.go#Reinstall`（生效路径）
- 要点：行内编辑明文；空密码合法写入 `config.yaml`（经快照 `env` 扁平键回显 `app.env`，对齐原型 applyStateChange 空密码分支）；重置回 `123456`；零校验零加密（§1.5）；**密码经容器 `env` 注入、只在建容器时落定**，故写成功后如实提示需「重建生效」（`svc.passwordPending`），数据服务卡片提供该入口（卷与宿主目录保留）
- 验收：空/超长/特殊字符/含空格密码四种保存-回显一致；改密码后不重建则运行容器仍用旧值（界面不得谎报已生效），点「重建生效」后容器 `env` 与配置逐字一致且数据卷不变
- **2026-09-22 修正（服务卡片三处失真：重建 preflight、端口显示、重置报成功）**：① `useModals` 的「重建生效」确认框曾带 `field:'port'` 去跑 `update-config`——端口键未落库时该键为空，裁决必然失败；而密码/扩展场景根本没有 `port` 可言。现只带 `kind` / `version`（占用判定由 `Reinstall` 内的 `assertHostPortsFree` 以 `store.CollectUsedPorts` 兜底），顺带删掉失效的 `envKeyPort` 导入。② `config.FlatEnv` 只在 `port > 0` 时才吐 `{KIND}_{VER}_PORT`，读不到键时 `ServiceView.portValue` 曾用 `suggestPortFor` 现算——mysql 8.4 显示 3384，而后端真容器发布的是 `registry.Spec.HostPort` 3306。现 `SvcMeta` 新增 `defaultPort`（与后端同值：php 0 / mysql 3306 / pgsql 5432 / redis 6379 / nginx 80）作显示回退，`SitesView.nginxPort` 同源；`suggestPortFor` 只留在 `InstallModal` 的预填用途。③ `PasswordField.reset()` 原先忽略写入结果、无条件弹「已重置」——失败时界面谎报成功。现 `persist` 返回布尔，仅在写成功时报成功

**T505 · service-config 保存链路** ｜ 依赖：T504
- 交付：`config_service.go`、`steps_config.go`、`ConfigModal.vue` 接真
- 要点：多文件勾选保存（files 列表）→ 保存前备份原文件 → 重启建议提示（`rw.applyHint`）；**服务宿主端口同密码一样只在建容器时发布**，改端口走 `update-config` 占用裁决（报 `portInUse` 不顺延），落进容器靠 `Reinstall`（§5.13.4 幂等「重装」；占用预检排在 Pre-Clean 之前，冲突即在跑容器不受影响）
- 验收：改 my.cnf 保存 → 重启后配置生效；失败回滚原文件；改端口后 `Stop`/`Start` 端口不变、`Reinstall` 才发布新端口；目标端口被占时重建被拒且原容器仍健康

**T506 · M5 集成验收** ｜ 依赖：T501–T505
- 验收：§10.5 清单签认；三服务与 PHP 站点联通（WordPress 实例化冒烟）

---

## 阶段 M6 · 运维面

**T601 · PHP 扩展离线缓存链路** ｜ 依赖：T303
- 交付：`internal/cache/extension_cache.go` 接真、`internal/service/extension_service.go`（任务编排在服务层内联，无独立 `steps_extension.go`）、`PhpExtensionsModal.vue` 接真
- 要点：严格按 §5.14.2「redis 扩展」流程：apk/pecl 双类型 lookup→临时目录编译→成功提升→必清临时→重建镜像→重启容器；失败报人话不进缓存
- 验收：有缓存装扩展零网络；编译失败注入 → 临时目录为空、缓存无该条目、容器状态回滚
- **2026-09-22 修正（编译失败的回滚自身不得死锁）**：回滚第一步原按「原本启用过扩展」直接建 `phpo/php:{ver}` 容器，固化镜像被手工删除或换机没带过来时报 `No such image`——回滚失败，用户连「重新点一次扩展」这条恢复路都没有（§5.13.1 可恢复性）。现回滚目标与 T306 的装配镜像同口径**按本机实际态现取**（`prevRef` 先 `ImageExists` 探固化镜像，探不到退回基座 `php:{ver}-fpm`；探针报错上抛）。任务另带 `Meta{Type:"extensions", Kind:"php", Version}`，扩展任务在 php 版本卡片上点亮「运行中/排队中」（见 T210）。已知口径：若固化镜像被删**且**启用集未变，`Apply` 因「无变化」直接返回不重建——恢复方式是勾掉再勾回任一扩展，属既定轻量限制。
- **2026-09-22 修正（扩展目录全量化 + 两处入口共用选择器 + 执行期逐行日志，AGENTS.md §5.16 / 决策 25）**：原实现只在前端铺了几颗示例 chip，「管理扩展」弹窗的列表更是只剩已启用项——用户既看不到本版本可用扩展的全貌，也无法从界面收敛扩展集，而容器内编译的整段输出被攒成一行日志，失败时只报「扩展安装失败」不出名字。现：① `frontend/src/constants/ext.ts` 成为**唯一目录**（73 项 / 8 组，内置 49 · pecl 24，11 项 `common`），后端 `config.ClassifyExt` 与之由**第 5 项门禁** `scripts/check-ext-catalog.go` 对账；② 新组件 `components/common/ExtPicker.vue` 供 `InstallModal`（预勾选常用）与 `PhpExtensionsModal`（预勾选当前已应用集）共用，支持分组/过滤/勾选常用/清空/目录外手填；提交语义是**完整集合**，后端 `diffExts(prev,next)` 自算增删，两者皆空即不建任务不重建容器；③ 执行期日志逐行进抽屉——`engine.ExecStream`（`stdcopy.StdCopy` 去 8 字节帧头、stdout/stderr 分流）是唯一 exec 出口，无换行的超长进度按 4 KiB 强断；④ 失败先落一行 `err` 点名扩展，再把 toast 消息收紧成「扩展 {name} 安装失败，本次扩展集未应用」，弹窗不关闭。停用 = 删 `/usr/local/etc/php/conf.d/docker-php-ext-{name}.ini`（真机取证：镜像内**不存在** `docker-php-ext-disable`）。用例：`internal/service/extension_service_test.go` 的逐行日志与去帧断言、`container_wire_test.go` 的 stdout/stderr 分流。
- **2026-09-22 产物级走查取证（同上一条修正的前端三面）**：走查通道是同哈希 `pnpm build` 产物 + 本地静态服务 + 浏览器驱动 Pinia store，取到的是**DOM/像素级证据**，不是真宿主原生窗口。已核对：① 安装弹窗（php）扩展区铺出 **8 组 / 73 项**（基础 39 · 数据库 10 · 缓存 5 · 图像 4 · 网络 9 · 异步 3 · 框架 2 · 调试 1），默认勾选 = `commonExts` **11 项**，弹窗宽度切到 `lg`；② 点选 `gd` 后计数 11→12、清空后「已选 0 / 目录 73」且等效命令的 `--ext` 段消失、`勾选常用` 一键复原（只增不删）；③ 过滤 `pdo` 收敛到 7 项 / 2 组，无匹配时给空态「没有匹配的扩展」；④ 版本可见性：`7.2` → 72 项（`ffi` 消失、`sodium` 保留），非数字版本 `客户站-legacy` → 不做限制仍是 73 项；⑤「管理扩展」弹窗默认勾选 = 本版本已应用集，未改动时底部按钮文案「当前配置」且 disabled；⑥ 目录外手填：非法名 `bad name!` toast「扩展名格式不正确」不入列，`my_custom-ext1` 入列即选中并归到「自定义（目录外）」分组，按钮改为「应用并重建（已选 16 个扩展）」；⑦ **失败不关弹窗**：应用失败后 `.modal-root.open` 仍在、错误原样 toast。**证据层级声明**：走查里 demo 通道的「已应用 15 项」是我为构造判据写入 store 的数据，不代表任何真机装机态；那次失败 toast 的内容是本地静态服务的 501 HTML（纯浏览器无 Wails runtime，非后端短消息），真宿主的失败消息字符串由 `extension_service_test.go` 锁死。截图 `/tmp/walk-g1-install-ext.png`、`/tmp/walk-g2-ext-fail-keeps-modal.png` 为本机临时件，不入仓。**仍不得据此声称「真宿主 GUI 已走查」。**
- **2026-09-23 修正（基座退回分支必须全量重编译，否则库里≠容器，AGENTS.md §5.16.2 / 总纲 v2.9.10）**：上一条「回滚不得死锁」的修法只补了**建容器用哪个镜像**，没补**装哪些扩展**。库里已启用过扩展、而固化镜像 `phpo/php:{ver}` 不在本机时（手工删镜像、换机没带过来），容器按基座 `php:{ver}-fpm` 重建——基座**不含任何** `prev` 扩展，而编译步骤仍只跑 `diffExts` 算出的 `added`：`prev` 里那些既没被编译、又会在任务结尾被原样写进 `php_extensions` 表并随快照广播，界面从此显示「已启用」而 `php -m` 里没有（违反 §5.13.1 一致性），且用户看不出任何异常。现该分支把待编译集换成**完整目标集**（`added, removed = uniqueSorted(enabled), nil`）：`removed` 置空是因为基座本就不含那几项，无需再删 ini；同时给一行 `dim`「固化镜像不在本机…目标扩展集全量重编译」，不得静默换口径（§5.16.3）。用例 `TestExtension_Apply_BaseFallbackRecompilesFullSet` 锁死命令序列 + 落库集合 + 该日志行。顺带校正假件前提：`ImageExists` 原先只认**本次** commit，使「固化镜像已在本机」的正常路径无法构造，现加 `hasImages` 供用例显式声明本机已有镜像（`TestExtension_Apply_DisableRemovesIni` 据此走回正常支，断言未削）。

**T602 · 备份 / 恢复 / 删除备份** ｜ 依赖：T506
- 交付：`internal/service/backup_service.go`（备份/恢复任务编排在服务层内联，无独立 `steps_backup.go`）、`pkg/archive/targz.go`、`BackupView.vue` 接真
- 要点：备份含 PHPO_HOME 五服务目录 + 离线缓存 + WWW 站点 + SQLite 快照 + 明文 `config.yaml`（用户裁决：随包携带、恢复落回并 `ConfigStore.Reload` 热重载，docs/备份脱敏规范.md）；不含 Docker 镜像；恢复先清空目标命名空间再恢复数据再建容器（§5.13.11）；backup-delete 二次确认
- 验收：备份→异机恢复→站点可访问 E2E；恢复中断 → 可重入不产生半套资源
- **2026-09-22 修正（读不动的条目不得判死整包 + 数据服务逻辑导出，AGENTS.md §5.17 / 决策 26）**：真机报错 `打包归档 失败: open …/php/8.0/logs/access.log: permission denied`——容器内进程在宿主 bind 目录里留的文件由容器 uid 拥有、权限常为 `0700`/`0600`，冷拷贝本就拿不到，旧实现让**一个读不动的文件**掀翻整包。现两处收口：① `pkg/archive.Create` 签名改 `([]string, []Skip, error)`，单条目/单目录读不动即**跳过并记 `Skip{Path,Reason}`**、整包继续（写 tar 中途失败仍上抛——header 已声明的 Size 收不回，硬跳会产出解包即坏的归档）；`logSkips` 按**所在目录聚合**（每目录至多 5 项 + 一行总数）进抽屉日志。② 备份任务在**暂停服务之前**插「逻辑导出数据服务」步骤，把 mysql/pgsql/redis 的**在跑实例**导进归档 `dump/`（`mysqldump --single-transaction --routines --triggers --events --all-databases` / `pg_dumpall -U postgres` / `redis-cli --rdb` 容器内暂存后 `cat` 流回，`--rdb` 吐 `/dev/stdout` 必 `Invalid argument` 是真机取证）；单库失败只点名一行不判死整包，未运行的服务给一行 `dim`；口令作 argv 元素传、不拼命令行文本；stderr 不进转储文件。③ 恢复侧**不自动重放** `dump/`，但必须给一行明示未重放，不得静默。用例：`pkg/archive/targz_test.go`（跳过而非判死）、`backup_service_test.go`（dump 编排与容错）。
- **2026-09-22 修正（恢复重放不得静默降格）**：① 重放 SQL 逐句执行时**缺列即报错、整事务回滚**，不再跳过该句——此前一份比目标库新的归档会被恢复成「半套库」而界面报成功；② 目标库不存在时只读归档文件头判定：**空归档**放行（凭空建库无损失），**非空归档**拒绝（不得凭空建库覆盖一个从未存在过的库）；③ 无活库时跳过 `DELETE` 重放段，避免误报「恢复失败」而实际什么都没动。

**T603 · doctor 15 项全量** ｜ 依赖：T601
- 交付：`internal/service/doctor_service.go`、`OverviewView.vue` 接真
- 要点：§5.7 检查项逐条实现（含 Docker Hub 可达「已缓存版本仍可装」、缓存完整性、临时目录残留、孤儿资源、状态漂移）
- 验收：每项人为制造失败态，文案与建议逐条对照 §5.7；doctor 一键修复项动作可回滚

**T604 · 应用升级闭环** ｜ 依赖：T212
- 交付：`internal/updater/downloader.go` `verifier.go` `installer_windows.go` `installer_darwin.go` `installer_linux.go` `rollback.go`、`steps_update.go`、`UpdateModal.vue`、`useUpdater.ts` `updaterStore.ts`
- 要点：SHA256 + Ed25519 双校验缺一不可（硬红线 6）；公钥经 `internal/updater/embed.go` 的 `go:embed signing` 随包分发（路径为 `internal/updater/signing/public.key`，非 `build/signing/`），当前仍是占位公钥——持钥者用 `scripts/sign-release.sh` 导出真公钥一次性提交，`scripts/verify-signing-guard.sh` 反推比对，不匹配即拒绝发布；`update:progress` 分阶段上报；失败回滚旧版
- 验收：篡改包 → 校验拒绝；升级中断 → 下次启动 marker 触发自动回滚；Linux 真机装/覆盖升级/回滚已验（`docs/T705-Linux真机冒烟记录.md`），win/mac 冒烟待对应平台环境

**T605 · 清洁三模式 + 回收站 + 审计** ｜ 依赖：T603
- 交付：`internal/service/cleanup_service.go`、`engine/orphan.go` `audit.go` `trash.go`、`store/trash.go` `operation.go`、`CleanupView.vue` `TrashViewer.vue` `CleanupModal.vue`（清理/回收站/审计逻辑内联于 `cleanup_service.go` 与 `engine/`，无独立 `steps_cleanup.go`）
- 要点：保守/标准/激进三模式（§5.13.6）；孤儿扫描启动+24h+手动，默认保留卷；审计 `<用户数据目录>/logs/operations.log`（Linux `~/.config/phpo/logs/`，JSON Lines，§5.13.10）；回收站 7 天；清理「激进」涉缓存需用户确认（§5.14.13）
- 验收：预置孤儿容器矩阵扫描命中；误删回收站恢复；每次写操作审计行可解析

**T606 · OfflineView 真数据化** ｜ 依赖：T601
- 交付：`internal/service/offline_service.go`（§5.14.10 的 12 个 API 全实现）、`api/offline.ts` `api/cache.ts`、`OfflineView.vue`、`CacheHitBadge.vue` `CacheDetailModal.vue` `CacheCleanupModal.vue`、`useCache.ts` `cacheStore.ts`
- 要点：统计/全部校验/三模式清理/单条删除/lookup/promote/clearTempDir 全接真；6 个 cache 事件全订阅展示
- 验收：安装任务中 `CacheHitBadge` 实时命中/未命中标记正确（未命中按 `action` 区分「本机重建 `local`」与「走网络 `pull`/`download`」，两者都不得显示为命中）；「全部校验」检出损坏条目并可清理

**T607 · 向导与装机体验** ｜ 依赖：T603
- 交付：`internal/service/wizard_service.go`、`HomeSetupWizard.vue` 接真
- 验收：新用户首启（NEEDS_HOME 15 动作全拦截提示）→ 向导完成 → 全站可用

**T608 · M6 集成验收** ｜ 依赖：T601–T607
- 验收：画像 F 断网全链路（装机→装服务→装扩展→建站→切版本→备份→恢复）零外网完成（缓存预置）；R1–R87 逐条回访记录；§10.5/§10.6 清单签认

---

## 阶段 M7 · 发布

**T701 · 三平台打包资源** ｜ 交付：`build/` 全量（appicon.png；windows 的 icon.ico/wails.exe.manifest/nsis；darwin 的 icon.icns/Info.plist/entitlements.plist；linux 的 phpo.desktop/icons/nfpm.yaml/postinstall.sh）；验收：三平台产物可安装可启动
**T702 · 签名与校验产物** ｜ 依赖：T701；交付：`scripts/sign-release.sh` `gen-checksums.sh` `verify-signing-guard.sh` `bump-version.sh` `version.sh`、`third_party/licenses/THIRD_PARTY_LICENSES.md`；验收：`.sig` 可被 updater 公钥验证；`checksums.txt` 全覆盖
**T703 · release.yml 一键出包** ｜ 依赖：T702；验收：tag 触发→三平台包+签名+checksums 附件（工作流含 `guard_only` dry-run 与三平台 matrix，均已落地；因未打过发布 tag、真公钥未提交，端到端出包待首次发布验证）
**T704 · 文档定稿** ｜ 依赖：T608；交付：§11.2 全部中文文档（现 `docs/` 32 篇，含用户手册与 M6/T705 真机验收记录）；验收：链接/术语/数字与总纲 §0.3 抽查一致
**T705 · 发布验收** ｜ 依赖：T701–T704；验收：Linux 真机 deb 安装/覆盖升级/卸载保留数据/回滚已验（`docs/T705-Linux真机冒烟记录.md`）；win/mac 安装器冒烟待对应平台环境

---

## 并行轨

**TX01 · CI 流水线**：`ci.yml`（M2 起逐段上线：单测→check-* 脚本→集成）`lint.yml` `release.yml`；门禁 = 每阶段验收工单
- **2026-09-21 修正（前端作业缺 TS 绑定）**：`frontend/bindings/` 是 `wails3 generate bindings` 生成物且被 gitignore，三条工作流在全新 checkout 上都没生成它就编译前端 ⇒ 实测 `pnpm -C frontend build` 以 26 行报错退出 2（**20 处** `TS2307 Cannot find module '../../bindings/…'` + 6 处派生 `TS7006`；17 个 `api/*.ts` 中 15 个静态导入绑定），`lint.yml` 的 `vue-tsc --noEmit` 同因。现把生成收进 `Taskfile.yml` 的 `frontend:build`（`deps: [bindings]`）——`build`/`package:*`/`release.yml` 一并转好；`ci.yml` 前端作业改跑 `wails3 task frontend:build`（并补 Go/webkit/Wails CLI 工具链），`lint.yml` 在 typecheck 前显式 `wails3 generate bindings -ts -clean`。
**TX02–TX05 · 校验脚本**：`check-i18n-keys.go`（T103 后）、`check-templates.go`（T307 后）、`check-docker-naming.go`（T304 后）、`check-cache-manifest.go`（T211 后）
**TX06 · 测试替身**：单测与包同目录（colocated），fake/mock 内联在各 `*_test.go`（`internal/{service,engine,cache,vhost,task,updater,store}` 等），无独立 `test/mocks/` 目录；真环境用例集中在 `test/integration/*_live_test.go`，由 `PHPO_LIVE=1` + Docker 可用性双重 skip 守护（`PHPO_LIVE=1 go test ./test/integration/ -run TestM3_Live -v`）——禁止为等真环境跳过离线可测的用例
**TX07 · docs 随阶段交付**：每阶段末更新对应 `docs/` 专项文档，M7 仅定稿不补写

---

## 里程碑依赖总览

```
M1 ──► M2 ──► M3 ──► M4 ──► M5 ──► M6 ──► M7
              │               │            ▲
              └─(T303 缓存管道被 M5/M6 复用)┘
升级检查(T212) ─────────────────────────────┘（M6 补全安装链）
```

> 工单粒度以「单 PR 可验收」为准；开工前按 §3.4.4 将所选工单拆为失败用例 → 实现 → 转绿三步。
