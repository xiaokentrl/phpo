# phpo 任务工单（逐阶段展开）

> **文档类型**：实施工单清单（派生文档，从属 `AGENTS.md` 总纲 v2.9.3 与 `实施顺序.md`）
> **编号规则**：`T<阶段><序号>`；验收标准全部可执行（§3.4.4 目标驱动：先写用例，调试至通过）
> **字段约定**：依赖 = 前置工单；交付 = 涉及文件（对齐总纲 §4.1 目录树）；验收 = 成功标准（缺测试用例不得声称完成）
> **冲突裁决**：本文与 AGENTS.md 冲突时，一律以 AGENTS.md 为准

---

## 阶段 M1 · 骨架

**T101 · 后端 Wails3 骨架** ｜ 依赖：—
- 交付：`go.mod`（module phpo / go 1.27）、`go.sum`、`wails.json`、`Taskfile.yml`、`Makefile`、`main.go`、`app.go`、`.gitignore` `.editorconfig` `.golangci.yml` `.air.toml` `config.example.yaml`
- 要点：仅 GUI 入口，无 `cmd/` 目录（§5.1）；开发期热重载用 `task dev`（`wails3` beta 的 `dev` 参数与本机不匹配，不作为验收前置）
- 验收：本机 `wails3 build`/`task build` 出 Linux 二进制并可起空窗口；`CGO_ENABLED=0` 交叉编译在 CI/三平台打包工单（T701/T703）验证

**T102 · 前端工程骨架** ｜ 依赖：T101
- 交付：`frontend/package.json` `vite.config.ts` `tsconfig*.json` `index.html` `src/main.ts` `App.vue` `src/router/index.ts`
- 要点：Vue 3.5+ / TS 5.x / Pinia 2.x；10 路由对齐原型 `NAV`（sites/php/mysql/pgsql/redis/nginx/backup/offline/settings/overview）；`frontend/bindings/` 由 `wails3 generate bindings` 生成、禁止手改（§0.2 规则 13）
- 验收：10 路由全可达；`pnpm build` 无 TS 报错

**T103 · i18n 提取（前端）** ｜ 依赖：T102
- 交付：`frontend/src/locales/zh-CN.ts` `en-US.ts`
- 要点：从原型 `MESSAGES`（约 300+ 键）全量直译提取，键名不变；语言切换即时重渲染
- 验收：键数对齐脚本比对（zh-CN 与 en-US 键集合相等）；切换语言无缺键

**T104 · 主题系统** ｜ 依赖：T103
- 交付：`frontend/src/styles/themes/*.css`（6 套）、`stores/prefsStore.ts`
- 要点：`THEMES`(6) 全量迁移；偏好仅存 localStorage（§3.1 原则 5）
- 验收：6 主题逐屏对照原型截图无回归；重启后主题保留

**T105 · 布局与缩放** ｜ 依赖：T102
- 交付：`stores/layoutStore.ts`、`composables/useZoom.ts` `useResize.ts`
- 要点：侧栏宽/抽屉高滑杆 + 3 预设 + 重置；`UI_SCALE.snap` 7 档缩放与 resize 补偿；命令面板 layout/zoom 命令联动
- 验收：各档位缩放下模态拖拽 resize 无错位（对照原型 zoom 补偿逻辑）

**T106 · 视图迁移 ×11** ｜ 依赖：T104、T105
- 交付：`views/` 12 个 .vue = 11 路由视图（含 CleanupView、OfflineView）+ `ServiceView.vue`（服务页共用基座）
- 要点：M1 阶段数据源为 mock state；表格/卡片/行内编辑结构与原型 1:1
- 验收：逐屏 diff 清单全过；空态/异常态样式齐全

**T107 · 模态迁移 ×12** ｜ 依赖：T106
- 交付：`components/business/` 各 Modal（安装、建站、站点配置、伪静态、扩展、配置、危险确认、装机向导、主题、升级、清理、缓存详情/缓存清理等）+ `TrashViewer.vue` `DockerGate.vue` 壳；端口无独立模态（行内编辑走 `usePortSuggest`）、备份/恢复在 `BackupView` 内联
- 要点：模态外壳统一（可拖拽/resize/遮罩关闭）；M6 才接真实逻辑的仅做壳
- 验收：12 模态均可打开、表单即时校验行为与原型一致

**T108 · 命令面板** ｜ 依赖：T106
- 交付：`components/common/CmdPalette.vue`、`constants/cmd.ts`
- 要点：`CMD_ITEMS`(20) 全量；⌘1–9 路由跳转；layout/zoom 命令可用
- 验收：20 命令逐项触发验证；模糊搜索命中正确

**T109 · 日志抽屉（任务引擎 mock）** ｜ 依赖：T102
- 交付：`components/business/TaskDrawer.vue`、`stores/taskStore.ts`、`composables/useTask.ts`
- 要点：日志行 5 类型（cmd/meta/ok/dim/err）；任务状态 4 态（running/success/failed/cancelled）；「等效命令」标注「仅展示」，复制时 Toast「本产品不提供 CLI」（§1.4）
- 验收：mock 脚本逐行回放、取消按钮生效；**真实宿主下并发写不阻塞**——`task.Manager` 串行 FIFO，前一个任务在跑时再发起的写操作进队列（队列详情随 `state:changed` 的 `Snapshot.Tasks` 推送，排队项可 `CancelQueued` 撤回），同标签重复提交由后端 `ErrQueued` 当场拒绝；`preflight` 不再有 taskBusy 全局守卫，忙锁文案（taskBusy）只保留在无宿主的 demo 通道与托盘退出守卫

**T110 · 事件总线** ｜ 依赖：T101
- 交付：`internal/app/emitter.go` `di.go`、`frontend/src/api/events.ts`、`composables/useStateSync.ts`
- 要点：§5.6 事件表全量 17 个事件名先定义常量；M1 用后端 mock 定时器发射
- 验收：17 事件名收发逐一验证；前端只订阅、无本地乐观更新（硬红线 4）

**T111 · 托盘占位** ｜ 依赖：T104
- 交付：`internal/ui/tray.go` `window.go`、`AppTrayMenu.vue`
- 要点：托盘菜单（主题点选/开日志/立即备份/退出）；Linux libappindicator 缺失时降级（§8）
- 接线收口（2026-09-21）：`InstallTray(app, emitter, window)` 把主窗口 `AttachWindow` 给托盘（此前从未绑定，点图标恒为无操作），并注册 `WindowClosing` 钩子接管关闭按钮——驻留则 `Hide()`，否则 `app.Quit()`（`resideOnClose()` 纯函数裁决，`tray_test.go` 四例覆盖）；`App.SetTrayPrefs` 承接设置页两张开关（`prefsStore` → `api/state.ts#setTrayPrefs`，宿主就绪后起闸、`localStorage` 为权威）。真实托盘启动在 `app.Run()`（`SystemTray.New` 延后执行），故删掉了原先那句空操作的 `tray.Run()`
- 验收：三平台托盘图标+菜单可用；「最小化到托盘」开关生效

**T112 · M1 集成验收** ｜ 依赖：T101–T111
- 验收（里程碑门禁）：原型界面 1:1 运行；doctor 首检弹窗/HomeSetup 向导流程走通（mock）；§10.1 验收清单逐项签认

---

## 阶段 M2 · 底座（纯逻辑，零 Docker 依赖）

**T200 · model 层 DTO** ｜ 依赖：T101
- 交付：`internal/model/` 13 文件（service/site/task/backup/offline/snapshot/update/operation/resource/cache_entry/dto + cleanup/doctor）
- 要点：字段与原型 `state` 结构及 §5.6 事件载荷一一对应
- 验收：`go vet` 通过；JSON 序列化快照测试

**T201 · 路径纯函数** ｜ 依赖：T200
- 交付：`internal/config/paths.go` `config.go`
- 要点：`derivePaths`/`hostToContainer`/`MOUNTS`(5) 从原型逐行直译；`<用户数据目录>`（XDG，Linux `~/.config/phpo`）与 `./`（PHPO_HOME 根，默认值 `~/phpo`）双根目录规则（§4.2 / AGENTS.md §0.1.1）
- 验收：以原型函数输出为金样本的单测全过（含中文路径、Windows 盘符）

**T202 · 版本策略校验** ｜ 依赖：T200
- 交付：`internal/config/versions.go`（校验）、`pkg/version/compare.go` `semver.go`、建议版本在 `frontend/src/constants/service.ts` 的 `SVC_META.suggested`（建议值，非白名单；仓库内无 `configs/` 目录）
- 要点：仅路径安全校验：非空/无 `/` `\`/无 `..`/无 `\x00`/**≤128**/trim（§5.4）；禁止字符集白名单
- 验收：`internal/config/versions_test.go` 边界用例：128/129 字符、含点版本 `8.4.3`、`../etc`、含空格、空串

**T203 · 密码策略** ｜ 依赖：T200
- 交付：`internal/config/password.go`（genPassword）、`internal/config/configstore.go`（`services.{kind}.{version}.password`）、`frontend/src/components/business/PasswordField.vue`（壳）
- 要点：明文写 `config.yaml`（`<用户数据目录>/` = XDG 用户配置目录内 `phpo` 子目录，唯一配置权威；不再有 PHPO_HOME 根下的 `./.env` 与 SQLite env 表）；默认 `123456`；可空、长度不校验、UI 可查看（§1.5 六规则）；禁止任何加密/校验路径
- 验收：空密码、1 位、100 位、含空格密码写入读取一致；`config.yaml` 权限 600/NTFS ACL（§8）；快照 `env` 扁平键 `{KIND}_{VER}_PASSWORD` 回显与落盘一致

**T204 · 端口引擎** ｜ 依赖：T200
- 交付：`pkg/port/suggest.go` `probe.go` `sequence.go`、`internal/store/port.go`；占用判据在 preflight 内联复用 `store.CollectUsedPorts`（无独立 `internal/preflight/portprobe.go`），本机 TCP 实探 `port.Probe` 仅供 doctor 的「80 端口可用」检查
- 要点：站点端口默认 80；**新建站点**占用时**不顺延**——`Options.KeepOnConflict` 原样保留用户所填端口并置 `Result.Occupied`，交上层只告警 + 站点降级（§5.8）；**改已有站点**端口才在 1–65535 顺延首个可用（不报错、无窗口上限）；服务端口占用报 `portInUse`；冲突集单一来源 `store.CollectUsedPorts`（数据服务端口 + 既有站点端口，对齐原型 collectUsedPorts）
- 验收：占用矩阵单测——建站 80 占 → 端口仍为 80 且 `Occupied=true`（`TestValidateKeepOnConflict`）；改站点端口 80 占→81、80–179 占满→180 继续向上无耗尽错误（`TestFindNextAvailableUpThenWrap` / `TestValidateOccupiedAlwaysAdvances`）

**T205 · SQLite 存储层** ｜ 依赖：T200
- 交付：`internal/store/store.go` `sqlite.go` `migrate.go` `migrate/0001–0008.sql` `snapshot.go` `sync.go` `port.go` `operation.go` `trash.go` `offline.go` `backup.go`
- 要点：`modernc.org/sqlite`（CGO_ENABLED=0）；0002 离线表、0004 审计表、0005 缓存清单表、0006 站点伪静态列、0007 下线 `dir_ready`（改由快照按「两根已持久化 + 目录存在」派生）、0008 任务账本列（`task_id/label/logs`）；SQLite 仅存运行态，密码/端口/两根归 `config.yaml`；**延迟建库**——两根未写入 `config.yaml` 前不打开 DB（首启零落盘）
- 验收：迁移 up/down 往返测试；崩溃恢复（WAL）测试

**T206 · preflight 框架** ｜ 依赖：T201–T205
- 交付：`internal/preflight/preflight.go`（`Run` 分派 + `needsHome` 集合，无独立 `common.go`）`validators.go`、`pkg/errs/errors.go` `codes.go`
- 要点：17 action switch-case 路由 + NEEDS_HOME(15) 集合 + PF 错误码表 + 返回 `{ok,errors,warnings,adjusted}`；警告不阻止（§0.2 规则 16）；UI 即时反馈仅为预览，最终裁决在此（规则 14；落地方式见 `docs/接口契约.md` §1.1——`app.go` 的 `guard()` 在提交任务前调 `Run`，只消费 `errors`）
- 验收：17 action 空壳用例全注册；`TestActionAndNeedsHomeCounts` 断言 `len(AllActions) == ActionCount == 17`、`needsHome` 命中数 `== NeedsHomeCount == 15`（总纲 §0.3 权威值）；**裁决已接入生产**：`app.go` 的 `guard()`→`runGuard()` 在 18 个写入口调 `preflight.Run`（17 action 全覆盖，`Reinstall`/`SiteAddHosts` 有意例外），非 `Ok` 即拒绝且不创建任务，`Warnings` 不阻断；`TestGateEveryActionWiredInFacade` 逐 action 对账 `app.go` 源码，漏接即失败（见 `docs/接口契约.md` §1.1）

**T207 · preflight 服务类规则（6）** ｜ 依赖：T206
- 交付：`internal/preflight/rules_service.go`
- 范围：install / uninstall / service-start / service-stop / update-config / service-config
- 验收：正反用例各≥2（未装版本启动、卸载在用 PHP 被拦等）

**T208 · preflight 站点类规则（6）** ｜ 依赖：T206
- 交付：`internal/preflight/rules_site.go`
- 范围：site-add / site-remove / site-port / site-vhost / php-switch / rewrite；php-switch 必须校验容器在场且上游为 `php-{version}-fpm:9000`（硬红线 1）；根在 WWW_ROOT 外 → `PF.rootOutsideWww` 警告；**建站端口冲突走 `conflictKeepWarn`：保留原端口、只告警、不写 `adjusted.port`**（§5.8），**改站点端口走 `conflictAdvance`：顺延并写 `adjusted.port`**；nginx 未装是建站唯一服务门禁（阻断），nginx 未运行 / PHP 未装 / 端口占用只降级不阻断
- 验收：对齐原型 site-add/site-port 分支行为逐条回测（含 warnings 不 ok:false）

**T209 · preflight 运维/缓存类规则（5）** ｜ 依赖：T206
- 交付：`internal/preflight/rules_ops.go` `rules_cache.go`
- 范围：extensions / backup / restore / backup-delete / offline-prune
- 验收：17 计数收口（6+6+5）；offline-prune 禁止清「正在使用」缓存（§5.14.12）

**T210 · 任务引擎骨架（三段式）** ｜ 依赖：T206
- 交付：`internal/task/manager.go` `task.go` `step.go` `funcstep.go` `emitter.go` `rollback.go` `ledger.go`
- 要点：preflight → task → applyStateChange 唯一写路径（硬红线 5）；Step 接口五法 Name/Execute/Rollback/Cleanup/Cancelable（§5.13.3）；M2 阶段 Executor 为 Noop；Manager 为**串行 FIFO 队列**（并发写排队不报错，同标签重复提交 `ErrQueued`，队列内容随 `state:changed` 的 `Snapshot.Tasks` 广播）；任务收口写任务账本（`operations.task_id/label/logs`，日志尾部保留 500 行），账本失败不改变任务结果
- 验收：静态扫描脚本证明无旁路写（UI 层不得直改 store 状态字段）；task:log/progress/done 事件按协议发射；`duration` 为 `time.Duration` 纳秒；失败原因取末条 `err` 级日志 + 账本 `operations.error/logs`

**T211 · 离线缓存核心** ｜ 依赖：T201、T205
- 交付：`internal/config/offline.go`（缓存/临时目录路径常量，仓库内无 `configs/` 目录）、`internal/cache/` 10 文件（manager/lookup/manifest/image_cache/extension_cache/tempdir/promote/verifier/cleaner/stats）、`internal/model/cache_entry.go` 落表
- 要点：目录结构严格按 §5.14.2（含 manifest.json schema）；缓存命中优先级「离线缓存 > 本机 Docker 镜像库 > 网络」（第二级见 §5.14.3 v2.9.6：镜像未命中先 `ImageExists` 探本机，已有即零网络 `docker save` 重建缓存）；SHA256 校验在 `internal/cache/verifier.go`（直接用标准库 `crypto/sha256`，无独立 `pkg/hash/`）；ClearTempDir 四必清（成功/失败/取消/启动）+ 每次发 `cache:tempdir-cleared`；缓存与 Docker 资源解耦（§5.14.13）
- 验收：`internal/cache/cache_test.go` 十例全绿（命中/未命中提升/编译失败清临时/损坏回退/启动扫残留/无后端/保守清理/统计等）+ 端到端 `test/integration/m6_offline_live_test.go`；损坏缓存注入 → `cache:corrupted` + 落入未命中分支（先探本机镜像库再决定是否联网）

**T212 · 升级检查骨架** ｜ 依赖：T200
- 交付：`internal/updater/checker.go` `scheduler.go`（启动 + 24h 触发，暂只发 `update:available`）
- 验收：`internal/updater/updater_test.go` 以 `httptest` 假发布清单覆盖有新版/无新版/网络失败三用例；失败仅提示「不影响使用」（§5.7）

**T213 · M2 集成验收** ｜ 依赖：T200–T212
- 验收：无 Docker 环境 CI 全量单测通过；`scripts/check-i18n-keys.go` `check-templates.go` `check-docker-naming.go` `check-cache-manifest.go` 四脚本上线并纳入 CI；§10.2 验收清单逐项签认

---

## 阶段 M3 · 服务线（PHP + Nginx，首次接真实 Docker）

**T301 · Docker 客户端与前置检查** ｜ 依赖：T213
- 交付：`internal/engine/client.go` `health.go`
- 要点：Docker 未装/未运行则不能启动服务（硬红线 7），报人话 + 下载链接（§5.7）
- 验收：mock 三种 Docker 状态；版本 <20.10 仅警告

**T302 · 镜像 load/pull/save** ｜ 依赖：T301
- 交付：`internal/engine/image.go` `registry.go`、`pkg/dockerutil/imageref.go` `wait.go`
- 要点：SDK `ImageLoad`/`ImageSave`；进度流式上报
- 验收：本地 tar 载入→删除→再载入幂等

**T303 · 缓存优先安装管道** ｜ 依赖：T211、T302
- 交付：`internal/task/steps/steps_service.go`（install 段）
- 要点：严格按 §5.14.3 流程图：lookup→命中 load→发 `cache:hit`；未命中先 `ImageExists` 探本机镜像库——已有即 `docker save`（`cache:miss` 的 `action=local`，零网络）、没有才 `pull`（`action=pull`）→save→verify→promote→更新 manifest→清临时→发 `cache:miss`+`cache:promote`+`cache:tempdir-cleared`；任务开始先清残留临时目录
- 验收：断网 E2E——预置 `./offline/php/8.4/image.tar`（`./` = PHPO_HOME 根，装机向导选定）后安装零网络成功；无缓存但本机 Docker 已有镜像 → 零网络重建缓存并安装成功；两者都没有且断网 → 明确失败且无脏状态

**T304 · 容器/网络/卷装配** ｜ 依赖：T303
- 交付：`internal/engine/container.go` `network.go` `volume.go` `mount.go` `inspect.go`、`pkg/dockerutil/naming.go`（容器 inspect 在 engine 侧，`pkg/dockerutil/` 只有 naming/imageref/wait）
- 要点：命名 `phpo-{kind}-{version}` / `phpo-network` / `phpo-{kind}-{version}-{purpose}`；网络别名 `{kind}-{version}`（如 `php-8.4-fpm`）；挂载走 MOUNTS 表
- 验收：`check-docker-naming.go` 扫描无违规资源

**T305 · 幂等三阶段（Pre-Clean/Execute/Post-Verify）** ｜ 依赖：T304
- 交付：`internal/engine/idempotent.go` `verify.go` `cleaner.go`
- 要点：操作前清同名/冲突资源、操作后验证「Docker 实际状态 ≡ SQLite 状态」；失败全回滚（§5.13.3）
- 验收：install/uninstall/start/stop 各重复执行 100 次结果一致、零残留

**T306 · 状态校准 + 生命周期服务** ｜ 依赖：T305
- 交付：`internal/engine/calibrate.go`、`internal/service/lifecycle_service.go` `registry.go` `php_service.go` `nginx_service.go`
- 要点：启动时 + 每任务后 + 手动校准；stopped 状态反向推导（原型映射）；重装 = 卸载保留卷 + 安装复用卷（§5.13.12）
- 验收：手动 `docker kill` 后校准一致并产生漂移事件（`docker:state-drift`）

**T307 · 模板引擎** ｜ 依赖：T213
- 交付：`internal/template/embed.go` `render.go` `templates/`（php×2、nginx、mysql、pgsql×2、redis、vhost 共 7 文件，5 服务）
- 要点：`DEFAULT_CONFIGS` 5 服务 7 文件逐一对齐原型内容；按服务种类分不按版本分（§5.3）
- 验收：`check-templates.go` 渲染快照与原配置 diff 为空

**T308 · 取消与回滚真化** ｜ 依赖：T305
- 交付：`internal/task/steps/steps_service.go`（cancel/rollback 路径）
- 要点：任务取消 → 清临时目录（必清时机 3）→ 回滚到操作前；`cancelTask` API + 前端取消按钮接真
- 验收：pull 中途取消：无半截镜像、无临时目录残留、状态回原点

**T309 · M3 集成验收** ｜ 依赖：T301–T308
- 验收：PHP 7.4/8.1/8.3/8.4 与 nginx 真实装/卸/启/停；任意字符串版本号可装（路径安全内）；缓存四事件前端可见；§10.3 清单签认

---

## 阶段 M4 · 站点闭环

**T401 · vhost 管理器** ｜ 依赖：T309
- 交付：`internal/vhost/manager.go` `parse.go` `upstream.go` `rewrite.go` `sync.go` `validate.go`、`internal/template/templates/vhost.conf.tmpl`
- 要点：VHosts 9 方法后端化；`replaceListen`/`replacePhpUpstream`（正则 `set \$php_upstream [^;]+;`，上游 `php-{version}-fpm:9000` 保留点号，硬红线 1）；伪静态 `REWRITE_PRESETS`(9)；vhost 指令放开（§5.10）但写入前 `nginx -t` 必过（硬红线 2）
- 验收：`internal/vhost/{parse,rewrite,upstream,manager,sync}_test.go` 覆盖解析/改写（含注释嵌套、多 location、IDN 域名，如 `TestParse_IDNDomain`）；坏配置注入 → `nginx -t` 拦截不落盘

**T402 · hosts 管理（三平台提权）** ｜ 依赖：T401
- 交付：`internal/vhost/hosts/`、`internal/util/fs.go`
- 要点：Windows UAC / macOS osascript admin / Linux polkit（§8）；不可写时人话提示（§5.7）
- 验收：三平台加/删 `demo.test → 127.0.0.1` 实测；拒绝提权时站点仍创建成功仅警告

**T403 · site add / remove** ｜ 依赖：T401、T402
- 交付：`internal/service/site_service.go`、`internal/task/steps/steps_site.go`、`frontend/.../SiteAddModal.vue` 接真
- 要点：域名零限制（§5.11）；根路径允许 WWW_ROOT 外（警告）；幂等建站；删除走回收站（`engine/trash.go`，7 天）；**删站顺带回收该域名的 hosts 条目**（`steps.RemoveHosts`，提权被拒仅警告不阻断、回滚时加回，与建站同口径）；**建站失败连 hosts 一起回滚**（`steps.AddHosts.Rollback` 撤下本步写入的行——末步「发布端口到 Nginx」失败时不留指向未建成站点的孤儿解析；仅「真写入过」才撤，幂等跳过与提权告警分支不动文件）
- 验收：重复建站幂等；删站后 hosts/vhost/目录状态一致（hosts 不得残留指向已删站点的行）；回收站恢复用例；`TestSiteService_Add_RollsBackHostsWhenLaterStepFails` + `steps_site_test.go`（`TestAddHostsRollback*` / `TestRemoveHostsRollback*` 各 3 档）锁死两侧回滚对称

**T404 · 端口冲突 UI 闭环（建站告警降级 / 改端口顺延）** ｜ 依赖：T403
- 交付：`frontend/.../SiteAddModal.vue`、`composables/usePortSuggest.ts`（行内改端口入口在 `views/SitesView.vue` 端口列）
- 要点：**新建站点**端口占用**不顺延、不改用户所填端口**（§5.8）——`preflight('site-add')` 出 `conflictKeepWarn` 警告、不带 `adjusted.port`，经 DangerConfirm 告警后以降级态建站（目录/hosts/落库照常，vhost 暂不落盘、端口暂不发布）；**改已有站点端口**才消费 `preflight('site-port')` 的 `adjusted.port`（1–65535 首个可用），DangerConfirm 展示「80→81 已调整」，确认后 `setSitePort` 落库
- 验收：80 占用下建站 → 确认文案为降级告警、端口仍为 80、列表当场可见；行内改 80 → 展示顺延后的新端口，确认后落库发射 `state:changed`；腾出端口后任一站点写操作或 nginx 就绪（`ReconcileServe`）自愈发布

**T405 · PHP 切换 10 步流程** ｜ 依赖：T404
- 交付：`site_service.go#SwitchPHP`、`frontend/.../PhpSelect.vue` `usePhpSwitch.ts`
- 要点：严格按 §5.5 十步（Preflight→读取→计算→校验→写盘→nginx -t→reload→落地→返回→验证）；禁止模糊匹配（硬红线 1）
- 验收：8.3↔8.4 往返切换后 vhost 上游逐字符等于期望值；`nginx -t` 失败步骤回滚原 vhost

**T406 · 伪静态 + vhost 编辑 UI 闭环** ｜ 依赖：T405
- 交付：`RewriteModal.vue`、`VhostConfigModal.vue`、`api/config.ts`
- 要点：9 预设选择即时预览；手改 vhost 保存前 preflight + `nginx -t`
- 验收：每预设生成内容与原型 `REWRITE_PRESETS` diff 为空；手改坏配置被拦并展示 nginx 错误原文

**T407 · M4 集成验收** ｜ 依赖：T401–T406
- 验收：`demo.test` 建站→浏览器访问→切 PHP→伪静态→改端口 全链路真跑通；§10.4 清单签认

---

## 阶段 M5 · 数据服务

**T501 · MySQL 全生命周期** ｜ 依赖：T407
- 交付：`internal/service/db_service.go`（mysql 段）、`internal/config/configstore.go`（`services.{kind}.{version}.{password,port}` 落 `config.yaml`）、`internal/template/templates/mysql/my.cnf.tmpl`、`MysqlView.vue` 接真
- 要点：卷 `phpo-mysql-{ver}-data` 持久；initdb 目录挂载；镜像安装复用 T303 管道
- 验收：装启停卸 + 建库写数 + 卸载重装数据不丢

**T502 · PostgreSQL 全生命周期** ｜ 依赖：T501
- 交付：`db_service.go`（pgsql 段）、`internal/template/templates/pgsql/*.tmpl`×2、`PgsqlView.vue`
- 要点：`pg_hba.conf` 预设放行容器网段；同上卷保留策略
- 验收：同 T501；跨版本（16↔17）并存不串数据

**T503 · Redis 全生命周期** ｜ 依赖：T501
- 交付：`internal/service/redis_service.go`、`internal/template/templates/redis/redis.conf.tmpl`、`RedisView.vue`
- 验收：持久化开关、PING 探活、卷数据保留

**T504 · 密码 UI 完整落地** ｜ 依赖：T503
- 交付：`PasswordField.vue`（👁/📋/↺）、`api/env.ts`、`internal/service/env_service.go`、`internal/service/lifecycle_service.go#Reinstall` + `app.go#Reinstall`（生效路径）
- 要点：行内编辑明文；空密码合法写入 `config.yaml`（经快照 `env` 扁平键回显 `app.env`，对齐原型 applyStateChange 空密码分支）；重置回 `123456`；零校验零加密（§1.5）；**密码经容器 `env` 注入、只在建容器时落定**，故写成功后如实提示需「重建生效」（`svc.passwordPending`），数据服务卡片提供该入口（卷与宿主目录保留）
- 验收：空/超长/特殊字符/含空格密码四种保存-回显一致；改密码后不重建则运行容器仍用旧值（界面不得谎报已生效），点「重建生效」后容器 `env` 与配置逐字一致且数据卷不变

**T505 · service-config 保存链路** ｜ 依赖：T504
- 交付：`config_service.go`、`steps_config.go`、`ConfigModal.vue` 接真
- 要点：多文件勾选保存（files 列表）→ 保存前备份原文件 → 重启建议提示（`rw.applyHint`）；**服务宿主端口同密码一样只在建容器时发布**，改端口走 `update-config` 占用裁决（报 `portInUse` 不顺延），落进容器靠 `Reinstall`（§5.13.4 幂等「重装」；占用预检排在 Pre-Clean 之前，冲突即在跑容器不受影响）
- 验收：改 my.cnf 保存 → 重启后配置生效；失败回滚原文件；改端口后 `Stop`/`Start` 端口不变、`Reinstall` 才发布新端口；目标端口被占时重建被拒且原容器仍健康

**T506 · M5 集成验收** ｜ 依赖：T501–T505
- 验收：§10.5 清单签认；三服务与 PHP 站点联通（WordPress 实例化冒烟）

---

## 阶段 M6 · 运维面

**T601 · PHP 扩展离线缓存链路** ｜ 依赖：T303
- 交付：`internal/cache/extension_cache.go` 接真、`internal/service/extension_service.go`（任务编排在服务层内联，无独立 `steps_extension.go`）、`PhpExtensionsModal.vue` 接真
- 要点：严格按 §5.14.2「redis 扩展」流程：apk/pecl 双类型 lookup→临时目录编译→成功提升→必清临时→重建镜像→重启容器；失败报人话不进缓存
- 验收：有缓存装扩展零网络；编译失败注入 → 临时目录为空、缓存无该条目、容器状态回滚

**T602 · 备份 / 恢复 / 删除备份** ｜ 依赖：T506
- 交付：`internal/service/backup_service.go`（备份/恢复任务编排在服务层内联，无独立 `steps_backup.go`）、`pkg/archive/targz.go`、`BackupView.vue` 接真
- 要点：备份含 PHPO_HOME 五服务目录 + 离线缓存 + WWW 站点 + SQLite 快照 + 明文 `config.yaml`（用户裁决：随包携带、恢复落回并 `ConfigStore.Reload` 热重载，docs/备份脱敏规范.md）；不含 Docker 镜像；恢复先清空目标命名空间再恢复数据再建容器（§5.13.11）；backup-delete 二次确认
- 验收：备份→异机恢复→站点可访问 E2E；恢复中断 → 可重入不产生半套资源

**T603 · doctor 15 项全量** ｜ 依赖：T601
- 交付：`internal/service/doctor_service.go`、`OverviewView.vue` 接真
- 要点：§5.7 检查项逐条实现（含 Docker Hub 可达「已缓存版本仍可装」、缓存完整性、临时目录残留、孤儿资源、状态漂移）
- 验收：每项人为制造失败态，文案与建议逐条对照 §5.7；doctor 一键修复项动作可回滚

**T604 · 应用升级闭环** ｜ 依赖：T212
- 交付：`internal/updater/downloader.go` `verifier.go` `installer_windows.go` `installer_darwin.go` `installer_linux.go` `rollback.go`、`steps_update.go`、`UpdateModal.vue`、`useUpdater.ts` `updaterStore.ts`
- 要点：SHA256 + Ed25519 双校验缺一不可（硬红线 6）；公钥经 `internal/updater/embed.go` 的 `go:embed signing` 随包分发（路径为 `internal/updater/signing/public.key`，非 `build/signing/`），当前仍是占位公钥——持钥者用 `scripts/sign-release.sh` 导出真公钥一次性提交，`scripts/verify-signing-guard.sh` 反推比对，不匹配即拒绝发布；`update:progress` 分阶段上报；失败回滚旧版
- 验收：篡改包 → 校验拒绝；升级中断 → 下次启动 marker 触发自动回滚；Linux 真机装/覆盖升级/回滚已验（`docs/T705-Linux真机冒烟记录.md`），win/mac 冒烟待对应平台环境

**T605 · 清洁三模式 + 回收站 + 审计** ｜ 依赖：T603
- 交付：`internal/service/cleanup_service.go`、`engine/orphan.go` `audit.go` `trash.go`、`store/trash.go` `operation.go`、`CleanupView.vue` `TrashViewer.vue` `CleanupModal.vue`（清理/回收站/审计逻辑内联于 `cleanup_service.go` 与 `engine/`，无独立 `steps_cleanup.go`）
- 要点：保守/标准/激进三模式（§5.13.6）；孤儿扫描启动+24h+手动，默认保留卷；审计 `<用户数据目录>/logs/operations.log`（Linux `~/.config/phpo/logs/`，JSON Lines，§5.13.10）；回收站 7 天；清理「激进」涉缓存需用户确认（§5.14.13）
- 验收：预置孤儿容器矩阵扫描命中；误删回收站恢复；每次写操作审计行可解析

**T606 · OfflineView 真数据化** ｜ 依赖：T601
- 交付：`internal/service/offline_service.go`（§5.14.10 的 12 个 API 全实现）、`api/offline.ts` `api/cache.ts`、`OfflineView.vue`、`CacheHitBadge.vue` `CacheDetailModal.vue` `CacheCleanupModal.vue`、`useCache.ts` `cacheStore.ts`
- 要点：统计/全部校验/三模式清理/单条删除/lookup/promote/clearTempDir 全接真；6 个 cache 事件全订阅展示
- 验收：安装任务中 `CacheHitBadge` 实时命中/未命中标记正确（未命中按 `action` 区分「本机重建 `local`」与「走网络 `pull`/`download`」，两者都不得显示为命中）；「全部校验」检出损坏条目并可清理

**T607 · 向导与装机体验** ｜ 依赖：T603
- 交付：`internal/service/wizard_service.go`、`HomeSetupWizard.vue` 接真
- 验收：新用户首启（NEEDS_HOME 15 动作全拦截提示）→ 向导完成 → 全站可用

**T608 · M6 集成验收** ｜ 依赖：T601–T607
- 验收：画像 F 断网全链路（装机→装服务→装扩展→建站→切版本→备份→恢复）零外网完成（缓存预置）；R1–R87 逐条回访记录；§10.5/§10.6 清单签认

---

## 阶段 M7 · 发布

**T701 · 三平台打包资源** ｜ 交付：`build/` 全量（appicon.png；windows 的 icon.ico/wails.exe.manifest/nsis；darwin 的 icon.icns/Info.plist/entitlements.plist；linux 的 phpo.desktop/icons/nfpm.yaml/postinstall.sh）；验收：三平台产物可安装可启动
**T702 · 签名与校验产物** ｜ 依赖：T701；交付：`scripts/sign-release.sh` `gen-checksums.sh` `verify-signing-guard.sh` `bump-version.sh` `version.sh`、`third_party/licenses/THIRD_PARTY_LICENSES.md`；验收：`.sig` 可被 updater 公钥验证；`checksums.txt` 全覆盖
**T703 · release.yml 一键出包** ｜ 依赖：T702；验收：tag 触发→三平台包+签名+checksums 附件（工作流含 `guard_only` dry-run 与三平台 matrix，均已落地；因未打过发布 tag、真公钥未提交，端到端出包待首次发布验证）
**T704 · 文档定稿** ｜ 依赖：T608；交付：§11.2 全部中文文档（现 `docs/` 32 篇，含用户手册与 M6/T705 真机验收记录）；验收：链接/术语/数字与总纲 §0.3 抽查一致
**T705 · 发布验收** ｜ 依赖：T701–T704；验收：Linux 真机 deb 安装/覆盖升级/卸载保留数据/回滚已验（`docs/T705-Linux真机冒烟记录.md`）；win/mac 安装器冒烟待对应平台环境

---

## 并行轨

**TX01 · CI 流水线**：`ci.yml`（M2 起逐段上线：单测→check-* 脚本→集成）`lint.yml` `release.yml`；门禁 = 每阶段验收工单
- **2026-09-21 修正（前端作业缺 TS 绑定）**：`frontend/bindings/` 是 `wails3 generate bindings` 生成物且被 gitignore，三条工作流在全新 checkout 上都没生成它就编译前端 ⇒ 实测 `pnpm -C frontend build` 以 26 行报错退出 2（**20 处** `TS2307 Cannot find module '../../bindings/…'` + 6 处派生 `TS7006`；17 个 `api/*.ts` 中 15 个静态导入绑定），`lint.yml` 的 `vue-tsc --noEmit` 同因。现把生成收进 `Taskfile.yml` 的 `frontend:build`（`deps: [bindings]`）——`build`/`package:*`/`release.yml` 一并转好；`ci.yml` 前端作业改跑 `wails3 task frontend:build`（并补 Go/webkit/Wails CLI 工具链），`lint.yml` 在 typecheck 前显式 `wails3 generate bindings -ts -clean`。
**TX02–TX05 · 校验脚本**：`check-i18n-keys.go`（T103 后）、`check-templates.go`（T307 后）、`check-docker-naming.go`（T304 后）、`check-cache-manifest.go`（T211 后）
**TX06 · 测试替身**：单测与包同目录（colocated），fake/mock 内联在各 `*_test.go`（`internal/{service,engine,cache,vhost,task,updater,store}` 等），无独立 `test/mocks/` 目录；真环境用例集中在 `test/integration/*_live_test.go`，由 `PHPO_LIVE=1` + Docker 可用性双重 skip 守护（`PHPO_LIVE=1 go test ./test/integration/ -run TestM3_Live -v`）——禁止为等真环境跳过离线可测的用例
**TX07 · docs 随阶段交付**：每阶段末更新对应 `docs/` 专项文档，M7 仅定稿不补写

---

## 里程碑依赖总览

```
M1 ──► M2 ──► M3 ──► M4 ──► M5 ──► M6 ──► M7
              │               │            ▲
              └─(T303 缓存管道被 M5/M6 复用)┘
升级检查(T212) ─────────────────────────────┘（M6 补全安装链）
```

> 工单粒度以「单 PR 可验收」为准；开工前按 §3.4.4 将所选工单拆为失败用例 → 实现 → 转绿三步。
